# 古典音乐神经符号生成 —— 全项目技术梳理

> ⚠️ **本文件中的部分数字已被修订**（2026-09-11 第二轮）：
> 「恢复准确率 0.857」「边界 F1 0.08–0.35」「末音长音 88%」等来自**有同曲窗口
> 泄漏的划分**与**渲染前**口径。诚实口径与完整修订见
> [`05-评估修订报告.md`](05-评估修订报告.md)：恢复 **0.407 ± 0.021**、
> 边界 F1（±1 容差）**0.532 ± 0.030**、自洽边界 F1 **0.095 ± 0.034**。


> 更新：2026-09-11　|　起点：MoveBricksWorker/classical-music-ai-experiments（交接版）
> 本文档从零开始完整叙述：做了什么、为什么这样做、怎么实现的、最终数字。

---

## 一、项目起点

**交接版系统**：
- ChordGPTv4（152M，6 维功能和声 token，自回归）生成和弦
- MelodyGPT（65M，自回归）以和声为条件生成旋律
- 规则织体（阿尔贝蒂/破碎八度/琶音）+ 三轨 MIDI 输出
- 已知痛点：旋律自回归退化（和弦音间振荡）、Cadence 头失效、仅 C 大调、
  只有准确率指标

**交接文档四项建议**：① 旋律改非自回归扩散（GETMusic）② 乐理指标入报告
（MusicAIR）③ 防模仿证据（MusicLDM/DRMW）④ 曲式与交互（LLM 曲式层 /
DeepBach 重采样 / RL scorer）

---

## 二、工作总览

| 阶段 | 核心内容 | 关键产出 | 关键数字 |
|------|----------|----------|----------|
| 1. 评估体系 | 乐理指标 + 防模仿指标 + 报告工具 | `src/metrics/`（2 模块）、`evaluate_report.py` | 首次有可汇报的合格度/相似度数字 |
| 2. 模型重构 | 自回归 → 非自回归离散扩散（D3PM） | `melody_diffusion.py` + 训练脚本 | 振荡 7.3% → 2.3% |
| 3. 根因排查 | 六个训练/推理不一致问题 | 见"根因发现"节 | 管线振荡 46% → 23% |
| 4. 听感工程 | 八度/节拍/终止式/换气/调式 | `gen_full.py` 重构 + 验证脚本 | 大跳 37%→3%，三轨 0 偏格 |
| 5. 数据与句法 | 众赞歌句子级数据集 + 终止式标注 + 边界预测 | v8 模型 + `gen_sentence.py` | 乐句长度 11.5 vs 语料 11.4 |

---

## 三、第一阶段：评估体系（一切判断的依据）

原项目只有准确率，改动无法评估。先建立两条证据链：

**1. 乐理合格度**（`src/metrics/theory_metrics.py`，MusicAIR 口径）
- **key confidence**：时值加权音级直方图 × 24 个 Krumhansl-Kessler 调性
  剖面的 Pearson 相关（+ top1-2 差距）；
- **melodic smoothness**：平均音程 / step ratio（≤2 半音）/ 方向变化率 /
  大跳率（音级空间最大距离 6，阈值 ≥5）；
- **rhythm matching**：强拍起音率 / 长音强拍率 / 强拍空拍率（支持拍位偏移）；
- 全部按乐曲切分（`prev_pc=null`）逐曲评估再聚合。

**2. 防模仿证据**（`src/metrics/anti_imitation.py`，MusicLDM + DRMW 口径）
- **SIMAA@90/95**：生成旋律对训练窗口的最大三元组 Jaccard 相似度超阈值比例；
- **17 维特征 + 马氏距离**：12 维音级直方图 + 平均时值/密度/音程/step/leap，
  训练集拟合均值协方差，Welch t 检验 + 95 分位内含率。

**3. 配套**：`evaluate_report.py`（三列对比报告）、`analyze_endings.py`、
`analyze_structure.py`（SSM）、`analyze_syntax.py`（句法）、`export_samples.py`。

**同条件基线对比**（60 组真实和声窗口，同一 scorer）：

| 指标 | 人类语料 | 扩散 v1 | 旧 MelodyGPT |
|------|---------|---------|--------------|
| step ratio | 0.857 | **0.769** | 0.542 |
| 和弦音吻合 | — | **0.815** | 0.414 |
| 振荡比例 | 0.159 | **0.023** | 0.073 |
| 马氏距离 | 3.73 | **4.84** | 8.69 |

旧模型的问题第一次被量化：生成分布逼近 DRMW 的"随机"判定线（≥10）。

---

## 四、第二阶段：旋律模型重构（v1 → v8）

### 4.1 为什么换非自回归

两大顽疾都是自回归机制的必然症状：
- **振荡**：只看左历史，前期偏差被"自我确认"；
- **Cadence 头失效**：序列起点没有上下文，天生特殊。

### 4.2 核心机制（`src/model/melody_diffusion.py`）

- **D3PM 吸收态 [MASK]**：任意位置、任意比例（t~U(0.05,0.95)）掩码 + 恢复，
  没有特殊位置；
- **condition flags**：每位标注 pitch/rhythm 各自"已知/待生成"，防止把
  模型自己的猜测当真值；
- **双向 Transformer + 双头**（音高/节奏联合）；
- **两种采样**：MaskGIT 余弦调度（16 步，默认 0.14s/条）与 GETMusic 原文的
  D3PM 祖先采样（T=100，按转移矩阵后验 `p_keep=γ_t·ᾱ_{t-1}/γ̄_t`）——
  实测持平，说明瓶颈不在采样；
- **理论 scorer 解码侧引导**：末几轮按 `置信度 × exp(乐理bonus)` 重加权；
- **同音衰减** `repeat_damp`；
- **DeepBach 式局部重采样** `resample()`：改一个和弦只重生成对应小节；
- **上下文补全** `generate_with_context()`：任意 keep 掩码 + 真值种子
  （动机复用的基础设施）；
- **RoPE 旋转相对位置**（手写 Q/K 旋转注意力，GETMusic 的 Roformer 设计）；
- **结构流**：全曲位置/剩余/乐句距离/拍位/终止式（见各版本）。

### 4.3 版本演进（全部本地 4060 8GB 训练，7-15 分钟/次）

| 版本 | 配置 | 关键点 | 恢复准确率 | SIMAA@90 |
|------|------|--------|-----------|----------|
| v1 | 6.2M d=252/8L | 基础扩散 | 0.736 | 0.000 |
| v2 | 14M d=384/12L | 容量实验 | 0.902 | **0.150 背谱** |
| v3 | 14M + 拍位 | 节奏恢复 0.966 | 0.924 | 0.217 背谱 |
| v4 | 6.2M + 拍位 | 拍位变体 | 0.746 | 0.017 |
| v5 | 6.2M + **块恒定条件** | 修训练/推理不一致 | — | 0.000 |
| v6 | 6.6M + **结构流 + RoPE** | 全局结构感知 | 0.625* | 0.000 |
| v7 | 6.8M + **乐句流** | 换气/句末收束 | 0.822 | 0.000 |
| v8 | 7.4M d=320/6L + **终止式流 + 边界预测头** | 句子级 | **0.857** | — |

*恢复准确率随条件欠定程度变化，非直接可比。

---

## 五、第三阶段：六个根因发现（最有价值的产出）

**① 训练/推理条件分布不一致**（原项目顽疾的深层原因）
语料和声标注 98% 逐音变化（连续长度=4 全语料仅 1 例），管线却给每和弦
条件重复 4 次。**原版 MelodyGPT 有一模一样的写法**。修复：训练条件改
4 音块多数投票恒定（v5），完整管线振荡 46%→23%。

**② 位置编码外推失效**
可学习绝对位置只训过 0-15（窗口 16 音），管线生成 192 音时 16 号之后
用的是**从未更新的随机嵌入**。修复：RoPE 相对位置。

**③ 稀有条件属分布外**
语料把属和弦标为 M 三和弦（(D,M,7)=366 vs (D,dom7,7)=65），管线喂 dom7
触发同音嗡鸣。修复：类型重映射。

**④ 容量必须与数据匹配**
14M 在小数据上背谱（SIMAA 0.15-0.22），6.2M 不背谱——防模仿指标直接
用于模型选型。

**⑤ 数据里没有完整句子**
旧语料是 23.5 音的单乐句片段（50%<16 音、165 个单音碎片），结尾 52%
带休止+30% 长音（终止式切分痕迹）。模型从未见过完整句子 → 不可能学会
句子结构。修复：众赞歌句子级数据集（见第七阶段）。

**⑥ "句子"标注的层级歧义**（本地 LLM 审计发现）
规则（终止式）标 3.4 句/首、LLM（换气点）标 6.2 句/首，位置 2 音内吻合
55%；人类语料自身"乐句末音落在期望和弦音"仅 48-59%。**两个来源是不同
层级（乐句层 vs 动机层），不是对错分歧**——这是层次化标注的实证依据。

---

## 六、第四阶段：生成管线与听感工程（用户反馈驱动）

| 反馈 | 根因 | 修复 | 效果 |
|------|------|------|------|
| "诡异" | `polish_melody` 区段式八度拱形 | `assign_octaves_min_leap` 跳进最小化 | 整八度大跳 37%→0-2 处 |
| "拍号对不上" | 和弦时值非节拍化（0.25-4 拍任意值）+ 旋律按时值÷4 等分 | `snap_to_measures`（吸附 4/4 小节）+ 旋律量化八分网格 | 三轨 0 偏格 |
| "不会表达开始结束" | 位置外推 + 无全局结构 | RoPE + `pos_bin`/`toend_bin` + 终止式公式（导音→主音、末小节短短短长） | 末音落主音 12%→100%、长音 0%→88% |
| "句子要喘气" | 无乐句级结构 | Ollama 标注乐句 + `phrase_bin` 流 + 句末 35% 气口 | 模型自发学会句末拉长（4.35 vs 句中 3.85，人类 4.13/3.74） |
| "和声雷同/无曲式" | I-IV-V-I 循环、无动机发展 | 真实语料进行采样（不同和弦 9.3→13.9/32、次属和弦 0→3.8）+ 曲式层（LLM 计划 + A-B-A' 动机复用 + 音区拱形） | 进行多样性翻倍 |
| "两个都是小调" | 数据集大小调近半半（194/174），随机落到小调；旋律调式倾向 | `--mode` 过滤 + 调式一致性评分（大调罚 Eb/Ab）+ 末音强制主音 | C major 置信 0.83/0.94 |
| "织体没了、一直在高音区" | 重构时漏掉织体轨；旋律八度基准 72（比原管线高八度） | 补回三轨 + 基准降到 64、上限 88→79 | 三轨齐全，旋律 C4-F5 |

---

## 七、第五阶段：数据与句法（句级重构）

### 7.1 句子级数据集（`build_chorale_dataset.py`）

music21 离线自带巴赫众赞歌 → **368 首完整作品**（中位 46 音、4.4 乐句/首、
12-24 小节），全部移调到 C（保留调式）：
- 旋律 = Soprano；和声 = chordify + 罗马数字分析（degree→func: 1/3/6→T,
  2/4→PD, 5/7→D；quality+seventh→类型表）；
- 乐句边界 = **终止式理论真值**（全 1358/阻碍 175/变格 48/半 42），
  边界吸附到附近时值最长的旋律音。

### 7.2 v8 模型（句子级）

- **终止式流**（5 类）+ **边界预测头**（辅助任务）；
- **结构流随机丢弃 50%**：防评估泄漏（否则边界可从 phrase_bin 直接读出，
  首版实测 F1=1.0 即是泄漏）；
- 32 音窗口（≈2-3 个乐句，模型第一次看见完整句子）；
- 结果：恢复 0.857，**边界 F1 0.08-0.35（高精确/低召回，随划分波动）**。

### 7.3 两级生成（`gen_sentence.py`）

- **规划器** `compute_sentence_plan`：和声进行 → 终止式到达点与类型
  （半终止仅在属和弦不立即解决时单列）；
- **实现器**：众赞歌和声框架 → 真实乐句/终止式计划 → v8 多候选生成
  （终止式落点和弦音评分 + 调式一致性）→ 跳进最小化八度 + 终止音修正
  + 句末气口 → **三轨 MIDI**（旋律/织体/和声垫）。

### 7.4 句法指标（`analyze_syntax.py`）

| 指标 | 生成 | 语料基线 |
|------|------|----------|
| 乐句长度（音） | 11.5 | 11.4 ✓ |
| 相邻乐句轮廓相似度 | 0.513 | 0.565 ✓ |
| 终止式实现率 | 0.35 | 0.53（语料自身） |
| 边界 F1 | 0.08-0.35 | — |

---

## 八、当前系统全貌

### 8.1 常用命令

```bash
# 句子级生成 (推荐): 众赞歌和声框架 + 终止式计划 + v8 生成, 三轨 MIDI
python gen_sentence.py --mode major --seed 5

# 训练句子级模型 (本地 8GB GPU, ~15 分钟)
python train_v8_chorales.py --epochs 120 --batch 64 --d 320 --layers 6 \
    --struct-drop 0.5

# 语料/合成和声的两条旧管线 (v7 模型)
python gen_standalone.py --harmony corpus --form llm --mode?   # 独立生成器
python gen_full.py                                             # ChordGPTv4 和声

# 评估
python analyze_syntax.py      # 句法指标
python evaluate_report.py     # 乐理 + 防模仿证据链
python analyze_endings.py     # 结尾质量
python analyze_structure.py   # SSM 结构分析

# 数据/标注
python build_chorale_dataset.py   # 众赞歌句子数据集
python annotate_phrases.py        # 乐句标注 (本地 Ollama)
python audit_phrases.py           # 标注质量审计
```

### 8.2 核心文件

```
src/model/melody_diffusion.py    # 扩散模型 (D3PM/flags/RoPE/结构流/边界头/重采样)
train_melody_diffusion.py        # v1-v7 训练
train_v8_chorales.py             # v8 句子级训练 (含防泄漏)
gen_sentence.py                  # 句子级生成器 (三轨) ← 当前主入口
gen_full.py / gen_standalone.py  # v7 管线 + 句子规划器 + 听感工程
motif_form.py                    # 曲式层 (LLM 计划 + 动机复用 + 音区)
src/metrics/*                    # 乐理指标 + 防模仿指标
build_chorale_dataset.py         # 众赞歌句子数据集
annotate_phrases.py / audit_phrases.py   # 本地 LLM 标注与审计
analyze_syntax.py / analyze_endings.py / analyze_structure.py / evaluate_report.py
02-新工作进展与结果.md            # 逐次修改的技术日志 (八修)
data/processed/chorales_sentences_v1.json  # 368 首完整作品 + 终止式标注
```

### 8.3 本地模型基础设施

- Ollama + `qwen3.5:9b-q4_K_M`（本机 8GB 可跑）
- **关键坑**：思考型模型默认内容进 `thinking` 字段（单次 33-87s），
  `think: false` 后 2 秒/次 → 标注 9-18 首/分钟，全程零 API 成本

---

## 九、局限与下一步

**当前局限**：
1. **句法理解仍弱**：边界 F1 波动大（0.08-0.35），终止式实现率 0.35
   （语料自身 0.53）——模型只部分内化了句法；
2. **标注层级未分离**：动机层（LLM 换气点）与乐句层（终止式）混用，
   应做**层次化标注**（动机⊂乐句⊂句子）并分别作为条件流；
3. 和声自动标注有噪声（chordify 逐音变化、乐句末音与到达和弦吻合率低）；
4. 数据规模：368 首众赞歌（单声部化），尚未利用更多语料；
5. 无人类听测数据。

**建议下一步（按价值排序）**：
1. **层次化标注 + 双层级条件流**：用众赞歌的乐句（终止式）+ LLM 的动机层
   各标一套，模型同时感知两个层级——这是审计发现的直接落地；
2. **边界 F1 稳定化**：增大边界损失权重、多阈值/多划分评估、按终止式
   类型分层报告；
3. **终止式实现率提升**：解码侧加强终止式 scorer（落点必落主音/属音）、
   或在训练中加入"终止式一致性"辅助损失；
4. **扩展语料**：music21 其他语料（莫扎特奏鸣曲、贝多芬四重奏）+ 现有
   众赞歌的多声部信息；
5. 人类听测（47 人回忆实验 / 7 点 Likert，模板已在论文集中）。

---

*和声是语法，乐句是句子，曲式是篇章。语法已经会了，句子刚学会认，篇章还没开始。*
