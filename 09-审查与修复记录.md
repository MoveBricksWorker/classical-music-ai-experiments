# 审查与修复记录（第三轮）

> 时间：2026-09-12　|　触发：用户要求"审查项目"
> 范围：全仓库代码 + 报告数字与产物的对应关系
> 产物：7 处代码修复、2 个新实验脚本、8 个新/更新的评估产物、若干文档口径修正
> （README 末尾「迭代 11」是同一份内容面向仓库读者的版本）
>
> **一句话**：代码与数据工程的底子是好的（测试全过、多数数字能复现），
> 但抓到 **两处"训练/推理不一致"**（项目自己的第 1 条铁律）、
> 一处使所有采样型脚本**不可复现**的全局随机源、以及
> **三处报告数字与产物对不上**——其中一处是任务混淆（拿"无旋律从零生成"的
> 0.319 去比"给旋律配和声"的条件）。

---

## 一、发现与处理一览

| # | 发现 | 影响 | 处理 |
|---|------|------|------|
| 1 | `evaluate_melody.gen_melody_scored` 待生成位置填 `REST/HOLD`，训练用的是 `MASK/9` | 报告 08 §6 唯一的正面结果（评分引导）是在分布外输入上算的；音域跨度 27.8 的异常由此而来 | ✅ 已修，重跑：音域 **27.8 → 11.2**，末音主音 0.50 → **0.62** |
| 2 | `gen_satb` 真值时值/已知声部节奏写死 `bin 4`（1 拍） | A/B 试听的真值参照 MIDI 长音被截断；模型的"已知 Soprano"节奏与训练不符 | ✅ 已修，重生成 5 首留出曲样例 |
| 3 | `gen_from_scratch` / `analyze_v10_decode`：`h=args.layers`（头数取自层数） | `--layers≠8` 直接崩；默认 8 掩盖 | ✅ 已修（加 `--heads`） |
| 4 | `build_windows` 用**全局** `random.shuffle` | 窗口顺序依赖进程随机状态 → `gen_satb --n 3 --heldout` 每次抽到不同曲子、`analyze_*` 每次抽样不同（实测同一命令两次的 argmax 准确率 0.898 vs 0.919） | ✅ 已修（局部 `Random(0)`），并给 `analyze_v10_decode` 加 `--out` |
| 4b | 同样的原因使"多候选择优"的幅度不可复现 | 四次不同抽样上平行五的降幅是 0.243→0.154、0.143→0.077、0.033→0.011、0.143→0.110（**方向一致、幅度差 3 倍**） | ✅ 已用固定顺序重跑并落盘（`v10_decode_experiment_v10aug.json`）；结论只能写"稳定降低违规"，**不能引用单次幅度** |
| 5 | 单声部模型无法走项目自带评估；`eval_recovery` 会把**真和声**喂给"无和声条件"的旋律模型；只 seed torch 不 seed random | v13 的掩码恢复数字被污染且不可复现 | ✅ 已修（`--solo-voice` 生效、`--eval-harmony-unknown`、双随机源固定） |
| 6 | 规划器采样用 `min(sample, N-2)` 夹取，会产出 `func=6`/`type=8` 等**不存在的标签** | 把模型从未当作目标的类别喂给实现器 | ✅ 已修（`sample_plan_token` 屏蔽非法/未知 id） |
| 7 | `key_conf_chorale` = 有符号 C 度，且 `startswith('C')` 把 `C#` 也算 C | "调性置信 0.152" 被读成"调性模糊"，实际是"40% 的曲子跑到了 G 大调" | ✅ 改为同时报 `key_c_ratio` / `key_conf_abs_mean` / `key_dist` |
| 8 | `corpus_reference()` 不产出"乐句末低音在主/属音"，人类基线每次打印 `nan` | 0.550 这个数没有对照 | ✅ 已补（真巴赫 0.634） |
| 9 | 产物无运行元数据；输出路径硬编码，任何一次运行都会**静默覆盖**历史产物 | 审查中我真的覆盖了 `v13_full_eval.json`（已用 git 还原）；`melody_eval.json` 的 v13 基线此前已被覆盖丢失 | ✅ 产物写入 config；`evaluate_melody`/`analyze_v10_decode`/`gen_from_scratch` 支持 `--out`；`train_v10_satb` 加 `--export-dir` 与 `best_epoch` |
| 10 | `requirements.txt` 一半是未使用的历史依赖，缺 `paramiko` | 新环境装不上云端工作流要用的包 | ✅ 已按实际 import 重写 |

---

## 二、证据修正：三处"报告数字与产物对不上"

### 2.1 「无和声 0.319」是**另一个任务**的数字（最重要）

报告 08 §3 的表格把三个条件并列：

| 条件 | 与真值逐音吻合 | 纵向和弦率 |
|------|---------------|-----------|
| 真和声标签 | 0.878 | 0.889 |
| 规划器生成的和声 | 0.247 | 0.814 |
| **无和声（v11 从零）** | — | **0.319** |
| 真巴赫 | — | 0.856 |

但 0.319 是 `v11_scratch_eval.json` 里 **"不给旋律、不给和声、四声部全从零写"**
（`gen_from_scratch.py`）的结果；而表格里另外两行是**"给定真旋律，只补内声部"**。
**两个不同任务的数字被放在同一张表里比较**，于是得到"和声由模型生成后
0.814 逼近真巴赫 0.856，远好于无和声 0.319 —— '散'的问题解决了"这个结论。

同一任务下的正确对照（新脚本 `analyze_planned_harmony.py`，留出曲 22 首 / 48 窗）：

| 实现器 | 条件 | 内声部逐音命中 | 纵向和弦率 |
|--------|------|---------------|-----------|
| v11_scratch | 真和声 | 0.813 | 0.900 |
| v11_scratch | **规划器和声** | 0.257 | **0.861** |
| v11_scratch | **和声未知** | 0.474 | **0.857** |
| v10_aug_vl20 | 真和声 | 0.876 | 0.872 |
| v10_aug_vl20 | 规划器和声 | 0.210 | 0.822 |
| v10_aug_vl20 | 和声未知 | 0.119 | 0.599 |
| 真巴赫 | — | — | 0.856 |

**读法**：
1. 给了真旋律之后，"无和声"并不散（v11_scratch 0.857）——报告里 0.814 vs 0.319
   的巨大差距是任务混淆造成的；规划器和声的真实增益是 **+0.004 和弦率**；
2. 更值得注意的是**规划器和声反而让逐音命中从 0.474 掉到 0.257**：
   规划器给的那条进行，与实现器自己在"和声未知"时内化的进行不一致
   —— 这正是"三级模型各自独立训练、没有联合微调"的直接证据（交接文档 P2）；
3. **实现器的训练条件是决定性的**：v10_aug_vl20（报告 07 的头牌，真和声条件下
   0.876）一旦撤掉和声标签就塌到 0.599；v11_scratch 稳得住（0.857）。
   流水线默认用 `v11_scratch.pt` 是**正确选择**，不要换成 v10_aug_vl20。

### 2.2 「掩码恢复 0.25 → 0.56」无法复现

报告 08 §6 写"给旋律喂真和声：掩码恢复 0.25→0.56"。本次用项目自己的
`eval_recovery`（win 64/stride 8、22 首留出曲、随机掩码）测同一对检查点：

| 模型 | 和声条件 | harmonize | infill | random |
|------|---------|-----------|--------|--------|
| v13_melody | 未知（它的训练条件） | **0.740** | 0.698 | 0.710 |
| v13_melody | 真值（它没见过的条件） | 0.504 | 0.443 | 0.497 |
| v16_mel_harm | 真值（它的训练条件） | **0.864** | 0.855 | 0.866 |
| v16_mel_harm | 未知（它没见过的条件） | 0.189 | 0.163 | 0.204 |
| v16_mel_harm | 未知，**win 48** | 0.226 | 0.191 | 0.256 |

即：文档里的 **0.25 恰好等于"把和声条件撤掉后 v16 的成绩"**（错条件），
而 **0.56 在任何配置下都测不出来**。结论方向（给真和声能大幅提升掩码恢复）
是对的，而且比文档写的更强（0.74 → 0.86）；但**这两个数字不能用**。
新口径已经写进产物：`data/processed/melody_eval_v13.json` /
`melody_eval_v16_harm.json` 里的 `mask_recovery` + `mask_recovery_cond`。

### 2.3 「严格口径 0.43–0.48（三折交叉验证）」此前查无实据 → 现已落盘

原文见 README 第 456 行与交接文档 §0/§4，但全仓库 `kfold_by_piece` 只用于 v9
旋律模型，`v10_multisplit.json` 是"同一模型换 3 个划分"（不是交叉验证）且数字是
0.699±0.104 / 0.877±0.056。本次给 `train_v10_satb.py` 加了 `--folds/--fold`
（按曲分组、每折重训、与训练同预算），实测见 §五。

> ⚠️ 口径提醒：`v10_multisplit.json` 的 0.877±0.056 与本节的严格三折**不可比**
> ——前者是"同一个模型换 3 个划分评估"，后者是"每折重训"。
> 另一个更隐蔽的坑见 §5：**训练日志里的 0.43–0.48 用的是增广验证集**
> （混入移调副本），标准口径（未增广）要高得多（0.706）——两把尺子差一倍。

---

## 三、代码修复明细（可复核）

| 文件 | 修改 |
|------|------|
| `evaluate_melody.py` | ① `gen_melody_scored` 待生成位置改喂 `MASK`/`num_rhythm`；② 加 `--out`、`--recovery-harmony {auto,unknown,true}`、`--no-recovery`，产物带 `_config`；③ 掩码恢复并入产物；④ 修 `sum(x for x in phr_end_stable)` 的写法 |
| `gen_satb.py` | 真值/已知声部时值改用 `dur_to_bin(s['dur'])`（与训练 `piece_to_window` 同源） |
| `gen_from_scratch.py` | ① `h=args.layers` → `--heads`；② 调性指标改多口径（`_key_stats`）；③ `corpus_reference` 补终止式落点；④ 产物带 config、支持 `--out` |
| `gen_full_pipeline.py` | ① 规划器采样改 `sample_plan_token`（屏蔽非法 id）；② 指标表与产物接入新调性口径 + config |
| `train_harmony_planner.py` | 新增 `N_FUNC_LABEL`/`N_TYPE_LABEL`/`sample_plan_token`（唯一实现，两个调用点共用） |
| `train_v10_satb.py` | ① `--solo-voice` 在 eval/export 路径生效；② `--eval-harmony-unknown`；③ `eval_recovery` 双随机源固定；④ `--folds/--fold/--seed` 按曲 k 折；⑤ `--export-dir`；⑥ 报告记录 `best_epoch`/`best_metrics`；⑦ `build_windows` 改局部 Random（可复现）；⑧ 报告加 `n_val_pieces` |
| `analyze_v10_decode.py` | ① `h=args.layers` → `--heads`；② `--out`（不再覆盖历史口径） |
| `requirements.txt` | 按实际 import 重写，补 `paramiko` |

**验证**：`tests/test_new_modules.py`（7 项）、`tests/test_rendering.py`（3 项）、
`tests/test_pipeline_diffusion.py` 全部通过；所有被改脚本 `--help` 冒烟通过；
单声部 `--eval-only` 冒烟通过（修复前必崩）。

---

## 四、本次新增/更新的产物

| 产物 | 内容 |
|------|------|
| `analyze_planned_harmony.py` | **新增**：真和声 / 规划器和声 / 未知和声 三条件对照（给流水线里程碑补证据链） |
| `data/processed/v13_planned.json` | 重写（带 config）：v11_scratch 三条件，见 §2.1 |
| `data/processed/v13_planned_v10aug.json` | **新增**：v10_aug_vl20 同条件对照 |
| `data/processed/melody_eval_v13.json` | **新增**：v13 原样（含掩码恢复与新口径） |
| `data/processed/melody_eval_v13_scored.json` | **新增**：评分引导（修正后的解码） |
| `data/processed/melody_eval_v16_harm.json` | **新增**：v16 和声锚定 |
| `data/processed/v13_full_eval.json` | 更新：新调性口径 + config + 人类基线终止式项 |
| `data/processed/v10_decode_experiment_v10aug.json` | **新增**：解码择优复现（带 config，不再覆盖旧口径） |
| `data/processed/v10_cv_summary.json` | **新增**：严格三折汇总（`analyze_cv_folds.py` 大样本复评） |
| `data/generated/satb_*.mid` | 重生成：真值时值修正后的 A/B 试听对 |
| `data/generated/full/full_1-5.mid` | 重生成：修正采样后的端到端成品 |
| ~~`data/processed/melody_eval.json`~~ | **删除**：内容是被覆盖过、且用错误输入算出的评分引导结果；已由上面三个 `melody_eval_v13*/v16*` 取代（git 历史仍可查） |

### 音乐性指标新口径（v13，8 首 × 16 小节）

| 指标 | 真巴赫 | 原样 | +评分引导（旧，错输入） | **+评分引导（修正后）** |
|------|--------|------|------------------------|------------------------|
| 级进率 | 0.911 | 0.625 | 0.696 | **0.706** |
| 音域跨度 | 11.8 | 12.5 | 27.8 ✗ | **11.2** ✓ |
| 乐句末落稳定音 | 0.507 | 0.156 | 0.375 | **0.313** |
| 末音落主音 | 0.840 | 0.125 | 0.500 | **0.625** |

→ **修掉输入约定后，"音域爆炸"的异常消失**，句末收束的改善依然成立
（末音主音 0.125 → 0.625，人类 0.840）。报告的结论不用推翻，
但那组数字要换成这一列。

### 端到端（模板 → 旋律 → 和声 → 四声部，5 首 × 16 小节）

| 指标 | 生成 | 真巴赫 |
|------|------|--------|
| 落在 C 调（含 c 小调）比例 | 0.600 | 0.785 |
| 调性明确程度（平均置信） | 0.811 | 0.862 |
| C 度（有符号，旧口径） | 0.178 | 0.503 |
| 纵向音响可解释为和弦 | 0.838 | 0.856 |
| 乐句末低音在主/属音 | 0.650 | 0.634 |
| 平行五度 /100 对 | 1.706 | 0.030 |
| 声部交越 /100 对 | 0.746 | 0.850 |

**新的读法**：调性不是"模糊"（明确程度 0.811 vs 人类 0.862，很接近），
而是"**跑到 G 大调**"（5 首里 3 首 C、2 首 G）；且 368 首语料里有 134 首是
c 小调，**生成端从没出过小调**——这是比"置信 0.152"有用得多的诊断。

---

## 五、严格三折交叉验证（新增产物）

配置：众赞歌、`win 64 / stride 8`、移调增广 ±2、`vl-weight 2.0`、
**每折 60 epoch（本机预算）**、按曲 3 折（seed 42）、单步 argmax 解码、
和声条件 = 真值。`analyze_cv_folds.py` 复评（标准口径用全部留出窗口，无抽样；
增广口径按训练日志同法抽 48 窗）。

| 验证集口径 | 折 0 | 折 1 | 折 2 | 均值 ± 标准差 |
|-----------|------|------|------|--------------|
| **未增广（标准：音乐按原样，都在 C）** | 0.849 | 0.571 | 0.699 | **0.706 ± 0.114** |
| 增广（±2 移调副本混入 = **训练日志口径**） | 0.454 | 0.440 | 0.381 | 0.425 ± 0.032 |

三个读法：

1. **训练时打印的那个 0.43–0.48 是"增广验证集"口径**：训练脚本把移调副本也放进
   验证集，等于要求模型"在别的调里也能配和声"——难得多。报告 07 的 0.877 走的是
   `--eval-only`（未增广窗口），所以**这两个数从来不是同一把尺子**；
   交接文档里那句"严格口径 0.43–0.48"很可能就是从训练日志里抄的
   （本次训练日志 0.454/0.440/0.381，与它吻合到 ±0.02）。
2. **严格口径的诚实值：0.706 ± 0.114**。
3. **折间标准差 0.114 才是真问题**：每折 92 首留出曲，配和声难度从 0.57 到 0.85
   ——"单划分数字不可信"不只是小验证集的毛病，而是**语料本身按曲分层**；
   报告 07 的 0.877±0.056 正落在最高那一折，属于偏易的 1/3。
   → 以后引用绝对水平，必须写清"哪一个划分 / 哪一折"。

> 与报告 07 的 0.877±0.056 的关系：那是**同一个模型换 3 个划分评估**（22 首），
> 本表是**每折重训、只在该折留出曲上评估**（各约 92 首）。
> 两者不可比，报告里必须分开写。本表预算 60 epoch（头牌模型约 300），
> 因此 0.706 是偏保守的下界。

---

## 六、仓库卫生（未改动，仅记录）

- `data/processed/long_sequences_v3.json` 与 `v4.json` 字节相同（各 4.5MB），
  `melody_full_v3.json` 与 `melody_llm_full_v2.json` 字节相同（各 1.8MB）
  —— 但分别被 `train_big.py` / `gen_standalone.py` 等引用，**删除会破坏脚本**，
  保持原样（如需瘦身，应改成其中一份 + 符号链接或统一加载函数）。
- 权重、日志、临时产物的 ignore 规则完备（`.gitignore` + `.git/info/exclude`）。

## 七、仍未做（建议下一步）

1. **旋律模型的训练命令仍未落盘**：`v13_melody.pt`（d=384/6L/1 声部）的训练
   报告与命令在云端，未随权重带回；本次只能从行为反推它的和声条件被抹掉
   （无真和声时 0.740、喂真和声反而降到 0.504）。建议补一次重训并落盘报告。
2. **人类听测**（P3）仍未做；本次修好了试听用的真值参照 MIDI，现在做 A/B 才可靠。
3. 节奏词表上限 4.0 拍、且 3.0 拍不在词表内（最接近 2.0）——真值里 3 拍长音
   渲染出来是 2 拍，属于**量化误差**而非 bug；若要精确，需要扩词表。
4. 规划器与实现器的**联合微调**（§2.1 第 2 点提供了量化动机：规划条件反而降低逐音命中）。

## 八、复现命令

```bash
# 单元测试
python tests/test_new_modules.py && python tests/test_rendering.py

# 严格三折交叉验证（每折约 21 分钟，本机 4060）
for k in 0 1 2; do
  python train_v10_satb.py --corpus chorales --epochs 60 --batch 32 \
      --win 64 --stride 8 --aug-transpose 2 --vl-weight 2.0 \
      --folds 3 --fold $k --seed 42 --out v10_cv_fold$k.pt --tag v10_cv_fold$k --n-export 0
done
# 每折用大样本复评
python train_v10_satb.py --eval-only --folds 3 --fold 0 --ckpt v10_cv_fold0.pt \
    --eval-n 400 --d 480 --layers 8

# 规划器和声三条件对照
python analyze_planned_harmony.py --n 48 --realizer v11_scratch.pt
python analyze_planned_harmony.py --n 48 --realizer v10_aug_vl20.pt \
    --out data/processed/v13_planned_v10aug.json

# 旋律（原样 / 评分引导 / 和声锚定）
python evaluate_melody.py --ckpt v13_melody.pt --n 8 --out data/processed/melody_eval_v13.json
python evaluate_melody.py --ckpt v13_melody.pt --n 8 --scored --out data/processed/melody_eval_v13_scored.json
python evaluate_melody.py --ckpt v16_mel_harm.pt --n 8 --with-harmony --out data/processed/melody_eval_v16_harm.json

# 端到端 + 试听成品
python gen_full_pipeline.py --n 5 --bars 16
python gen_satb.py --n 3 --heldout --ckpt v11_scratch.pt

# 单声部模型评估（修复后才可用）
python train_v10_satb.py --eval-only --solo-voice 0 --eval-harmony-unknown \
    --ckpt v13_melody.pt --d 384 --layers 6
```
