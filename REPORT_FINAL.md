# 古典和声智能生成系统 — 最终项目报告

## 一、项目定位

本项目探索**神经符号音乐生成（Neuro-Symbolic Music Generation）**：将 Transformer 模型的统计学习能力与古典音乐理论的结构化知识相结合，构建一个可解释的小型音乐生成系统。

与追求"最大模型生成任何音乐"的主流方向不同，本项目关注：**小模型 + 音乐知识 + 可解释生成**。

## 二、系统架构

### 2.1 和弦模型 (ChordGPTv4)

- **架构**：16层 Transformer Decoder，d=768，152M 参数
- **输入/输出**：6维结构化 token（功能、和弦名、时值、拍位、转位、终止式）
- **训练数据**：461首古典作品，31651个和弦（LLM 标注）
- **准确率**：93.5%（功能预测）
- **核心创新**：将传统和声分析中的语义变量显式编码为生成 token（**symbolic representation innovation**）

### 2.2 旋律模型 (MelodyGPT)

- **架构**：16层 Transformer Decoder，d=504，65M 参数
- **输入**：和弦上下文（功能、类型、根音）
- **输出**：pitch class (0-11) + 旋律角色 + 节奏分类
- **训练数据**：28912个旋律音（LLM 标注，8种角色分类）
- **生成方式**：scorer 引导采样 + 反振荡硬拦截

### 2.3 织体生成

- 3种织体模式：阿尔贝蒂低音、破碎八度、琶音
- 乐句级切换（D/PD→T 规则分句）
- 固定节奏模式

### 2.4 MIDI 导出

- mido 直写三轨 MIDI（旋律 ch0 / 织体 ch1 / 和弦 ch2）
- 整数等分 slot 对齐（无浮点累积误差）
- 事件排序正确（note_off 优先于 note_on，避免 stuck note）

## 三、数据管线

```
乐谱(.krn/.mxl)
    ↓
LLM 和声分析 
    → chord_progressions (461首, 31651和弦, 6维标注)
    ↓
LLM 旋律分析 
    → melody_notes (28912音, 8种角色)
    ↓
训练 ChordGPTv4 + MelodyGPT (AutoDL 4090, ~¥5)
```

**教训**：高并发前先验证——100并发无缓存导致 API 击穿（$120）。

## 四、关键设计决策与迭代

### 4.1 旋律生成的演进

| 阶段 | 方案 | 问题 |
|------|------|------|
| V1 | MelodyGPT 直接生成 → argmax 采样 | 全是和弦音，无多样性 |
| V2 | + 12条硬规则后处理 | 规则修症状修不了病因 |
| V3 | 纯过程式生成 | 太呆板，无 ML 多样性 |
| V4 | 骨架+填充架构 | 太僵硬，失去模型贡献 |
| **V5** | **scorer 引导采样 + 反振荡拦截 + 多候选择优** | **当前方案** |
所有方案的效果都没有达到预期，但是v5的效果可以说是这几个里面最好的

### 4.2 Scorer 引导解码（核心创新）

不同于事后规则修正，scorer 在**采样过程中**介入：模型生成概率分布 → top-20 候选 → 乐理评分重排 → 采样。

评分函数：
```
+0.4  级进 (1-2半音)
+0.3  和弦音
+0.2  强拍和弦音
-0.3  大跳 (≥6半音)
-0.3  重复同音
-0.2  强拍非和弦音
```

### 4.3 反振荡拦截

检测最近4音是否仅在2个 pitch class 间振荡（且均为和弦音）→ 将这两个 pitch class 的概率压制至 0.01x，强制模型探索新音。

### 4.4 多候选择优

生成 3 个候选旋律 → 评分（级进率、重复率、和弦音比例）→ 选最优。将"生成后修正"改为"生成中引导 + 生成后选择"。

## 五、已知限制与未来方向

### 当前限制

1. **旋律质量**：MelodyGPT 存在自回归退化，scorer 引导缓解但未根除，质量极差
2. **仅 C 大调**：未实现移调
3. **固定节奏**：旋律使用固定节奏模式
4. **无动机发展**：缺乏主题-变奏-再现的结构

### 未来方向

1. **Melody Planner + Realizer 架构**：MelodyGPT 生成高层旋律规划（轮廓、张力曲线、骨架音），Realizer 解码为具体音符
2. **Beam Search + Melody Critic**：保留多条候选路径，用乐理评分选择最优
3. **Phrase State Conditioning**：加入乐句位置（开头/发展/高潮/终止）作为模型输入
4. **Motif Token**：加入动机重复/变化的显式标记

## 六、创新点总结

| 模块 | 创新程度 | 说明 |
|------|---------|------|
| 6维和声表示 | 中 | 结构化功能编码 |
| 和声优先生成 | 中高 | harmony-driven melody generation |
| 理论引导解码 | 高 | theory-guided neural decoding |
| 古典乐句约束 | 中高 | Schenkerian + counterpoint rules |
| 完整 MIDI 管线 | 工程创新 | 三轨钢琴 + 织体编排 |

**核心贡献**：探索了"如何让神经网络生成的旋律符合古典音乐语法"这一开放问题，并提出 scorer 引导采样作为理论引导解码的轻量实现方案。

## 七、成本总结

| 项目 | 金额 |
|------|------|
| DeepSeek 和声分析 | ￥150 |（不小心开的高并发加清缓存）
| AutoDL 4090 云训练 | ¥20 |
| **总计** | **￥170** |

---

*和声是语法，旋律是诗歌。语法错了诗歌再美也没用。*
