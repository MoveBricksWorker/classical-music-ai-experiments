# 从"配和声"到"生成"——三级生成流水线（v13）

> 时间：2026-09-12　|　触发：用户指出"我们的最终目标是做一个音乐生成模型"
> 产物：`train_harmony_planner.py`、`gen_from_scratch.py`、`gen_full_pipeline.py`、
> `harmony_planner_mel.pt`、`v13_melody.pt`

---

## 一、先承认问题：v10/v11 不是生成模型

| 成分 | v9 | v10 | v11（从零） |
|------|----|----|------------|
| 乐句/终止式计划 | 检索 | — | 规则模板 |
| 和声进行 | 检索 | 真值条件 | **未知（模型必须自己定）** |
| Soprano 旋律 | 模型 | 真值条件 | **未知（模型必须自己写）** |
| 内声部 | 规则 | 模型 | 模型 |

v10 是**配和声器**：旋律、和声、时间网格全由外部给定，模型只补内声部。
v11 试图让四声部模型"全掩码从零写"，结果**很差**：

| 指标 | v11 从零生成 | 真巴赫 |
|---|---|---|
| 纵向音响可解释为和弦 | **0.319** | 0.856 |
| 调性置信（C 大调） | 0.300 | 0.503 |
| 平行五度 /100 对 | 0.848 | 0.030 |
| 声部交越 /100 对 | 5.196 | 0.850 |

**诊断**：模型要在同一个注意力里同时决定"这里是什么和弦"和"四个声部怎么落音"，
而和弦标签此前的训练里 70% 的窗口都是喂给它的。让它一次学会两件事，数据与
训练量都不够。

---

## 二、拆成三级：每一步都有真值可监督

```
曲式模板（规则：4 句、半终止/全终止位置、长度）        ← 唯一的外部输入
    ↓ ① 旋律模型      只训 Soprano，无和声条件
Soprano 旋律
    ↓ ② 和声规划器    以旋律 + 模板为输入，输出每切片和弦(func/type/root)
和弦计划
    ↓ ③ 四声部实现器  以旋律 + 和声为条件，写 Alto/Tenor/Bass
四声部成品
```

每一步都是**可监督、可单独评估**的子任务 —— 这正是本项目"神经符号"的定位：
不是让一个网络端到端地猜，而是把音乐理论里的中间表示显式化。

---

## 三、中间里程碑：和声改为"模型生成"（旋律暂用真值）

> ⚠️ **2026-09-12 审查修正（见 `09-审查与修复记录.md` §2.1）**：本节原表的
> "无和声 0.319"取自 `v11_scratch_eval.json`，那是**"不给旋律也不给和声、
> 四声部全从零写"**的另一个任务；与"给定真旋律只补内声部"并列属于任务混淆。
> 下表是同一任务（留出曲 22 首 / 48 窗，`analyze_planned_harmony.py`）的重测：

| 实现器 | 条件 | 内声部逐音命中 | 纵向和弦率 |
|--------|------|---------------|-----------|
| v11_scratch | 真和声标签 | 0.813 | 0.900 |
| v11_scratch | **规划器生成的和声** | 0.257 | **0.861** |
| v11_scratch | 和声未知 | 0.474 | **0.857** |
| v10_aug_vl20 | 真和声标签 | 0.876 | 0.872 |
| v10_aug_vl20 | 规划器生成的和声 | 0.210 | 0.822 |
| v10_aug_vl20 | 和声未知 | 0.119 | 0.599 |
| 真巴赫 | — | — | 0.856 |

- **规划器和声的真实增益是 +0.004 和弦率**（0.857 → 0.861），不是"0.319 → 0.814"：
  给了真旋律之后，纵向音响本来就不散，和弦率主要由旋律 + 实现器自身保证；
- 更值得注意的是**规划器的和声反而让逐音命中从 0.474 掉到 0.257** ——
  规划器给的那条进行与实现器自己在"和声未知"时内化的进行不一致，
  这是"三级各自独立训练、没有联合微调"的直接证据（交接文档 P2 因此从"可选"
  升级为"必做"）；
- **实现器的训练条件是决定性的**：报告 07 的头牌 v10_aug_vl20 在真和声下 0.876，
  撤掉和声标签就塌到 0.599；v11_scratch 稳得住（0.857）。
  流水线默认用 `v11_scratch.pt` 是正确选择；
- 规划器精度（留出曲，本次复现）：功能 0.765 / 类型 0.625 / 根音 0.641；
  **加旋律输入后功能从 0.630 提升到 0.771** —— 印证了"和声主要由旋律决定"。

---

## 四、评估口径的修正（同轮自查）

- **配和声准确率的可信口径**：严格"训练/评估分离"的三折交叉验证（每折重训）
  给出 **0.706 ± 0.114**（标准口径；`data/processed/v10_cv_summary.json`），
  而此前引用的 0.85 来自只有 22 首、恰好偏易的留出集
  （同一模型在 16 首上是 0.68、22 首上 0.86、全量含训练曲 0.95）。
  → 绝对水平按 0.7 理解；所有**相对结论**（移调增广 +0.29、解码器修复 4.5 倍、
  多候选择优降低违规）不受影响，因为它们都在同一划分上对比。
- ⚠️ **2026-09-12 审查补充（差一倍的尺子）**：本节原先写的 0.43–0.48 其实是
  **训练日志口径**——训练脚本把 ±2 移调副本也当验证窗口（等于要求"在别的调里
  也能配"），同一批检查点在该口径下是 0.425 ± 0.032，在标准口径（未增广）下是
  0.706 ± 0.114。两个数都对，但**不是同一把尺子**；引用时必须写明口径。
  另外折间从 0.57 到 0.85（每折 92 首留出曲），"单划分 0.877"是偏易的 1/3。
- **自由生成没有真值**，主指标换成音乐性指标 + 真巴赫基线：
  调性一致性、纵向音响可解释为和弦、终止式落点、声部进行违规、防模仿。

---

## 五、局限与下一步

1. 旋律模型（①）刚训练完，端到端结果待评估；
2. 曲式模板仍是规则（长度/乐句/终止式位置）—— 这是有意的设计选择
   （相当于"作曲上的形式要求"），若要连形式也生成，需要一个段落级规划器；
3. 三级模型各自独立训练，**没有联合微调**；误差会逐级累积
   （旋律不好 → 和声跟着偏 → 声部也跟着偏）；
4. 仍然没有人类听测；"好不好听"必须靠耳朵。

---

## 六、旋律模型的四组实验（2026-09-12，均为负结果）

三级流水线里**旋律模型是最弱一环**。用 `evaluate_melody.py`（配 368 首真众赞歌
Soprano 基线）量化后：级进率 0.63（人类 0.911）、乐句末落稳定音 0.16（人类 0.51）、
末音落主音 0.12（人类 0.84）。本轮试了四种改法：

| 实验 | 改动 | 级进率 | 句末稳定音 | 结论 |
|------|------|--------|-----------|------|
| v13 基准 | 368 条众赞歌旋律 | 0.625 | 0.16 | — |
| v14 | + Palestrina 最高声部（数据 ×9） | 0.587 | 0.12 | **更差** |
| v15 | 修假标签 + 两语料 1:1 | 0.548 | 0.12 | 仍无改善 |
| v16 | 给旋律喂真和声（和声锚定） | 0.677 | 0.24 | 掩码恢复 **0.74 → 0.86**（详见下方修正），自由生成基本不变 |

> ⚠️ **2026-09-12 审查修正**：原文写"掩码恢复 0.25→**0.56**"，这两个数字
> **无法复现**（用项目自己的 `eval_recovery`：v13 在和声未知下 0.740、
> v16 在真和声下 0.864；0.25 恰好等于"撤掉和声条件后 v16 的成绩"，
> 0.56 在任何配置下都测不出来）。结论方向不变且更强：**给真和声能把掩码恢复
> 从 0.74 提到 0.86**（两个模型各自的训练条件），但**自由生成的音乐性几乎不变**
> —— "能填空、不能作曲"的结论成立。新数字带口径落在
> `data/processed/melody_eval_v13.json` / `melody_eval_v16_harm.json`。

**v14 变差的根因（值得记）**：Palestrina 的"乐段/终止式"是按固定窗口切的**假标签**
（各声部轮流呼吸，很少同时休止），83% 的训练窗口带着假结构标签 → 模型学会
"忽略乐句/终止式条件"，把众赞歌的终止式信号一起丢了。
已按铁律修正为"没有意义的条件标**未知**"（`train_v10_satb.py`），1:1 配比后仍无改善。

**v16 的对照最说明问题**：给真和声后掩码恢复率从 0.25 跳到 0.56（说明旋律确实被
和声约束），**但自由生成的音乐性几乎不变**。→ 结论：**数据量不是瓶颈**，
模型"**能填空、不能作曲**"——条件预测 ≠ 自由生成，后者要求模型内化整个旋律风格分布。

### 唯一正面结果：乐理评分引导解码能修"局部语法"

在**绝对音高空间**评分（音级空间会跨八度）：级进 +0.4 / 大跳 −0.4 / 同音 −0.3；
乐句末 +0.6 落稳定音级、末音 +1.2 落主音。

| 指标 | 真巴赫 | 原样 | + 评分引导（旧，输入有 bug） | **+ 评分引导（修正后）** |
|------|--------|------|---------------------------|------------------------|
| 级进率 | 0.911 | 0.625 | 0.696 | 0.706 |
| 音域跨度 | 11.8 | 12.5 | 27.8 ✗ | **11.2** ✓ |
| **乐句末落稳定音** | 0.51 | 0.16 | 0.38 | **0.31** ✓ |
| **末音落主音** | 0.84 | 0.13 | 0.50 | **0.63** ✓ |

> ⚠️ **2026-09-12 审查修正**：`gen_melody_scored` 此前把待生成位置填成
> `REST/HOLD`（训练用的是 `MASK`/`num_rhythm`），实测两条输入路径的 argmax
> 只有 28% 一致 —— 旧那列是**分布外**算出来的，"音域跨度 27.8"的异常正是
> 由此而来。改用训练同源输入后重跑，**音域恢复正常、末音主音从 0.50 升到 0.63**。

→ 结论更新：**句末收束这类局部语法可以靠解码引导修好**（末音落主音
0.13 → 0.63，句末稳定音 0.16 → 0.31，人类 0.84 / 0.51），
「音域爆炸」是 bug 不是模型问题。剩下的真实短板是**级进率**（0.71 vs 人类 0.911）
与**句末稳定音仍只有人类六成**——这两项要靠模型本身，不是解码能补的。
