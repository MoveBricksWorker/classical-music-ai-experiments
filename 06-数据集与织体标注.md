# 数据集与织体标注说明（SATB 四声部 + 对位预训练语料）

> 时间：2026-09-12　|　产物：`chorales_satb_v2.json`、`palestrina_satb_v1.json.gz`、
> 织体标注审计 `texture_audit.json`
> 相关代码：`build_chorale_satb_dataset.py`、`build_palestrina_dataset.py`、
> `annotate_texture.py`
>
> **一句话**：把训练数据从"18.5k 个单声部音符"扩到 **66.9 万音符 / 四声部切片**，
> 并给每个乐句打上织体类型标签 —— 这是"从旋律生成走向多声部写作"的数据基础。

---

## 一、为什么做（v9 的数据瓶颈）

v9 的训练数据是 `chorales_sentences_v1.json`：368 首众赞歌的 **Soprano 单声部**，
共 18,547 个音符。这带来三个硬限制：

1. **信息利用率 25%**：巴赫众赞歌是四声部作品，此前只用了最高声部；
2. **模型学不到"声部独立进行"**：单声阶旋律序列里没有对位信息，
   而"听起来像古典音乐"的很大一部分来自声部间的独立与呼应；
3. **织体靠规则硬凑**：管线的伴奏音型是固定三条规则（阿尔贝蒂/破碎八度/琶音），
   用在巴赫众赞歌和声上属于风格穿越 —— 根因是没有"织体"这一层标注可学。

---

## 二、数据集 A：`chorales_satb_v2.json`（众赞歌四声部）

**来源**：music21 自带巴赫众赞歌（371 首，公开领域）。
**规模**：368 首成功（3 首缺标准四声部被跳过）。

| 项 | 值 |
|---|---|
| 曲目 | 368 首 |
| 纵向切片（每个 onset = 一个和声音响） | **30,095**（82/首） |
| 音符（实际起音，四声部合计） | **85,699** |
| 各声部音符数 | S 18,547 / A 21,773 / T 22,395 / B 22,984 |
| 乐句 | 1,608（4.4/首） |
| 拍号 | 4/4 × 329、3/4 × 37、3/2 × 1、12/8 × 1 |
| 平均同时发声音数 | 4.0 |

> **校验点**：Soprano 音符数 18,547 与 v1 数据集**完全一致** —— 说明四声部抽取
> 没有改变原有旋律层，v9 的乐句/终止式标注可以无缝对照。

**乐句与终止式**：直接复用 `build_chorale_dataset.py` 的规则探测（终止式 D→T /
D→vi / PD→T + 贪心分段），分布 authentic 1350 / deceptive 170 / plagal 46 / half 42
—— 与 v1 的 1358/175/48/42 一致（差异来自四声部切片对齐时的边界吸附）。

**字段结构**：

```jsonc
{
  "id": "chorale_0", "title": "...", "key_original": "G major",
  "style": "chorale", "time_signature": "3/4", "n_measures": 21,
  "voice_order": ["S", "A", "T", "B"],
  "slices": [                     // 纵向切片: 每个 onset 一条
    {"off": 0.0, "dur": 0.5,
     "midi":  {"S": 67, "A": 64, "T": 60, "B": 48},   // null = 该声部休止
     "attack":{"S": true, "A": true, "T": true, "B": true},  // 是否在此起音
     "n_voices": 4, "pc_collision": false, "uniform_pc": false, "doubling": false,
     "func": "T", "type": "M", "root": 0, "degree": 1}          // 和声 (与 v1 同口径)
  ],
  "phrases": [                    // 乐句级
    {"start_slice": 0, "end_slice": 14, "cadence": "authentic",
     "soprano_note_index": 9, "texture": "chordal",
     "features": {"coincidence": 0.73, "independence": 0.28, "uniform_ratio": 0.0,
                  "pedal_ratio": 0.17, "mean_voices": 4.0, "imitation_run": 2,
                  "n_slices": 15}}
  ],
  "bars": [{"bar": 0, "figure": "block", "n_notes": 4}]         // 小节级伴奏音型
}
```

---

## 三、数据集 B：`palestrina_satb_v1.json.gz`（对位预训练语料）

**来源**：music21 自带 Palestrina 全集（1318 个 `.krn`，19 世纪全集版，公开领域）。
**规模**：1236 首成功（82 首为三声部作品或声部配置异常，被跳过）。

| 项 | 值 |
|---|---|
| 曲目 | 1236 首 |
| 纵向切片 | **270,506**（219/首） |
| 音符（四声部合计） | **583,707** |
| 各声部音符数 | S 150,129 / A 162,258 / T 152,310 / B 119,010 |
| 段落（section） | 局部窗口见 §五统计 |
| 音域有序性（S>A>T>B） | **1061/1068 = 99%**（余下 7 首声部音域重叠，属正常） |

**为什么是它**：巴洛克众赞歌以柱式和声为主，学不到"声部独立进行"；
文艺复兴复调的织体 93% 是模仿/复调（见 §五），正好补这一课。
两套数据**字段结构一致**（只多 `style` 与 `voice_names`），
因此同一套模型可以"先在 Palestrina 预训练 → 在众赞歌微调"。

**与众赞歌的字段差异**：
- 无 `func/type/root/degree`（调式音乐，罗马数字分析无意义，置 null）；
- 新增 `style: "palestrina"`、`voice_names`（原始声部名）、`source`（.krn 相对路径）；
- 音高统一移调，使**终止和声的最低音 = C**（与众赞歌"全部移调到 C"同思路）。

**抽取细节（踩过的坑）**：
1. Palestrina 语料里 ~19% 是**三声部**作品，或声部名不规范（出现两个 `Tenor`）；
   只靠名字匹配会丢掉这些数据 → 增加**按平均音高排序的兜底**
   （最高音→S，最低音→B），失败率从 19% 降到 **6%**；
2. 各声部轮流呼吸，**很少同时休止** → 靠"同时休止"切段落时 1236 首平均只有
   1.0 段；增加**固定窗口兜底**（40 切片/段），使织体标签落到局部段落。

---

## 四、织体标注体系（本次新增的重点）

三个层级，全部由**规则**产生（可复现、可全量、零成本），Ollama 只用于事后审计。

### 4.1 切片级（每个纵向音响）

| 字段 | 含义 |
|---|---|
| `n_voices` | 同时发声音数 |
| `attack[v]` | 该声部是否在此切片起音（区分"新起音"与"延音"） |
| `pc_collision` | 是否有同音级重合（含八度加倍，四声部写作常态） |
| `uniform_pc` | 整片只有一个音级（真正的齐奏/八度齐奏） |
| `doubling` | 是否有八度加倍 |

### 4.2 小节级 `figure`（伴奏音型）

在**最低发声音部**上判定，且要求音高落在该小节的和弦音上（用切片级和声校验）：

| 标签 | 判定 |
|---|---|
| `block` | ≥60% 的切片有 ≥3 声部同时起音（柱式，四声部众赞歌的默认） |
| `alberti` | ≥6 个音且前 4 音含 3 个不同和弦音、之后按 4 音循环重复（根-五-三-五） |
| `broken` | 偶数位音高为同一音级、整体 ≥3 个不同音级（低音与上方音交替） |
| `arpeggio` | 同向跑动 ≥3 步、每步都是跳进（≥2 半音）、总跨度 ≥7 半音 |
| `sustained` | 无起音但有音在延续（长和弦/长音跨小节） |
| `moving` / `none` | 其他 / 真空白 |

> `alberti`/`broken`/`arpeggio` 在当前语料里几乎不出现（这正是问题所在：
> 众赞歌本来就不用钢琴织体），它们是为**下一步的钢琴语料**
>（莫扎特奏鸣曲、贝多芬四重奏）预留的检测器。

### 4.3 乐句级 `texture`（织体类型）

| 标签 | 含义 | 判定条件 |
|---|---|---|
| `chordal` | 柱式和弦（主调） | 同起音率 ≥0.55 |
| `imitative` | 声部间有动机模仿 | 存在 ≥4 音、时值一致、时差 0.5–4 拍的匹配串 |
| `polyphonic` | 节奏独立的复调 | 同起音率 <0.55 且独立性 ≥0.55 |
| `pedal` | 持续音 | 低音长音占比 ≥0.45 **且** 上方声部呈柱式（同起音率 ≥0.50） |
| `unison_octave` | 齐奏/八度 | 整片同音级的切片 ≥40% |
| `mixed` | 无主导类型 | 以上都不满足 |

支撑特征（全部写入数据，可自行改阈值重标）：

- `coincidence` 同起音率：≥3 声部同时起音的切片占比；
- `independence` 节奏独立性：1 − 各声部"起音时刻集合"两两 Jaccard 的均值；
- `imitation_run` 最长模仿串长度（音程+时值匹配，时差一致）；
- `pedal_ratio` 低音长音占比：低音 ≥2 拍的长音期间上方声部有 ≥3 次起音的时间占比；
- `uniform_ratio` 齐奏率：整片单音级的切片占比。

**阈值是调出来的，不是拍的**（记录过程，便于复核）：
1. 首版把"低音长音 + 上方声部在动"直接判 pedal → 在 Palestrina 上误判 **68%**
   （文艺复兴里低音长音是常态）→ 增加"上方声部须呈柱式"这一条，降到 7.5%；
2. 首版模仿匹配只要 3 个音程相同 → 众赞歌里两声部碰巧同向级进就误判
   → 改为"音程+时值都要匹配、时差在 0.5–4 拍内保持一致、≥4 个音"；
3. 首版 `alberti` 只要 4 个音构成 3 音循环 → 低音一小节 4 个音就误判
   → 提到 ≥6 音（至少 1.5 个循环）。

---

## 五、统计结果

**众赞歌（368 首 / 30,095 切片 / 85,699 音符）**

| 织体（乐句级，1,608 句） | 占比 | | 伴奏音型（小节级） | 占比 |
|---|---|---|---|---|
| `chordal` | 63.8% | | `block` | 58.8% |
| `mixed` | 24.3% | | `moving` | 38.7% |
| `imitative` | 6.3% | | `arpeggio` | 1.1% |
| `pedal` | 4.8% | | `broken` | 0.9% |
| `polyphonic` | 0.7% | | `sustained` | 0.4% |

**Palestrina（1236 首 / 270,506 切片 / 583,707 音符）**

| 织体（段落级，**7,156 段**，5.8 段/首） | 占比 | | 伴奏音型（小节级） | 占比 |
|---|---|---|---|---|
| `polyphonic` | 58.3% | | `moving` | 74.4% |
| `imitative` | 18.5% | | `block` | 24.7% |
| `pedal` | 12.6% | | `sustained` | 0.9% |
| `mixed` | 9.1% | | | |
| `chordal` | 1.5% | | | |

> 两套语料的织体分布正好互补：众赞歌 64% 柱式（主调合唱），
> Palestrina 77% 复调/模仿（文艺复兴对位）—— **一句话解释了这个项目
> 为什么"只会写柱式旋律 + 规则伴奏"：模型只见过柱式语料。**

---

## 六、标注审计（Ollama 本地模型）

按项目惯例（"任何标注都先审计再信"，见 00-交接文档 §6），用本地
`qwen3.5:9b-q4_K_M`（`think:false`）对规则标注做独立复核：

- 把四声部切片渲染成文本表（每行一个时间点，`C5` = 新起音、`C5~` = 延音、`-` = 休止），
  让模型在标签集内独立判定并给出理由；
- 分层抽样（每个规则标签都抽到），报告一致率、混淆矩阵与分歧案例；
- 代码：`annotate_texture.py`；结果：`data/processed/texture_audit.json`。

**审计的实际价值：它抓到了一个真 bug。** 首轮 5 例一致率 **0%**，
且模型理由高度雷同（都判 chordal）。人工核查发现：乐句记录里
`start_slice` 存的其实是"句末切片"，导致 `end_slice == start_slice`
—— 喂给模型的表**只有一行（一个和弦）**。修正乐句区间后重新审计，
一致率与结论见下节。

> 教训与项目既有经验一致：**评估/标注管线本身要先验证**。
> 如果只看"0% 一致"就下结论"LLM 不懂音乐"，就会错过真正的数据 bug。

### 审计结论：本地 LLM **不能**当织体标注器（但审计本身很值得做）

修正乐句区间后重跑（45 条分层抽样，覆盖全部标签）：

| 规则标签 | n | 与 LLM 一致 | LLM 最常给的标签 |
|---|---|---|---|
| `polyphonic` | 9 | **67%** | polyphonic ×6, pedal ×3 |
| `pedal` | 9 | **56%** | pedal ×5, polyphonic ×4 |
| `chordal` | 9 | 11% | polyphonic ×3, pedal ×3 |
| `mixed` | 9 | 11% | pedal ×5, polyphonic ×3 |
| `imitative` | 9 | **0%** | polyphonic ×4, mixed ×2, pedal ×2 |

总体一致率 **29%**。三个可诊断的原因（都对不上"规则错"这个结论）：

1. **LLM 的标签能力有限**：45 次里它从未输出过 `imitative`（也从未输出
   `unison_octave`）—— 从文本表格做**动机级**分析超出它的能力，所以
   `imitative` 的一致率必然是 0%，这不构成规则有误的证据；
2. **两者回答的不是同一个问题**：规则给的是"**主导**类型"（阈值作用在整个乐句），
   LLM 给的是"**最显眼**的特征"（低音一有长音就判 pedal、声部节奏一不同就判 polyphonic）。
   在粗粒度三类（chordal/polyphonic/pedal）内一致率也仅 **48%**，
   且 LLM 把 64% 柱式的众赞歌判成 45% polyphonic；
3. **LLM 过度自信**：置信度全部落在 0.80–0.95，与 29% 的实际一致率完全不匹配
   —— 不能拿它当"软标签"。

**因此本轮的处理是**：
- 把**特征**（`coincidence` / `independence` / `pedal_ratio` / `imitation_run` /
  `uniform_ratio`）当作**主标注** —— 它们客观、可复现、可重新阈值化；
  `texture` 硬标签只是按文档化阈值给出的一个便利视图；
- 真值仍缺：下一步应做 **100–200 句的人工金标准**（两人独立标 + 报告一致性），
  这是论文级的必要工作，也是唯一能校准阈值的方法；
- 结论：**不要花钱/时间用 LLM 做大规模织体打标**（本次 45 条约 7 分钟、
  一致率 29%）；它的价值在别处 —— 本次它抓出了管线里的真 bug。

---

## 七、复现命令

```bash
# 众赞歌四声部 + 织体标注 (约 8 分钟)
python build_chorale_satb_dataset.py --out data/processed/chorales_satb_v2.json

# Palestrina 对位语料 (约 10 分钟, 1236 首 / 927k 音符)
python build_palestrina_dataset.py --gzip

# 织体标注审计 (本地 Ollama, 约 7 分钟)
python annotate_texture.py --n 48 --batch 3
```

---

## 八、局限与下一步

**局限**
1. 和声标注（func/type/root）来自 `chordify` + 罗马数字，仍有噪声
   （语料自身"乐句末音落在到达和弦音"仅 48–59%，见 05 报告）；
2. Palestrina 无功能和声标注（调式音乐），只做织体/对位层；
3. 织体标签是**规则**产物，阈值虽经调参 + 审计，仍非人类标注；
   真正严谨的做法是人工标注 100–200 乐句作为金标准（下一步）；
4. 伴奏音型检测器（alberti 等）在当前语料上几乎没有正例，
   需要引入钢琴语料才能真正验证。

**下一步（数据 → 模型）**
1. 用 `chorales_satb_v2` 训 **v10 四声部模型**：先做最容易出效果的设定
   ——"给定三个声部，用扩散模型补第四个声部"（masked infilling，与现有 D3PM 框架兼容）；
2. 用 `palestrina_satb_v1` 做**对位预训练**，再在众赞歌上微调
   （同一 schema，只需加 style 条件流）；
3. 把 `texture` 作为**条件流**接入生成器，替换固定的三条织体规则
   —— 这是"织体标注"的直接用途：让模型学会"这段该用柱式/模仿/持续音"，
   而不是永远弹阿尔贝蒂；
4. 扩语料：music21 自带的莫扎特奏鸣曲（16 首）、海顿、舒伯特、舒曼
   （共约 60 首钢琴/室内乐作品，可用同一套抽取器，且**能真正激活
   `alberti`/`broken`/`arpeggio` 检测器**）。
