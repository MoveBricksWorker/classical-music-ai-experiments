# 古典音乐 AI 生成项目经验总结

> 从端到端模型到神经符号音乐生成的实践记录

## 项目背景

这个项目最初源于一个简单的问题：

> 当前的大规模音乐生成模型，究竟是在理解音乐，还是仅仅学习了大量音乐数据中的统计规律？

随着 Suno、MusicLM 等端到端音乐生成模型的发展，AI 已经能够生成具有较高听感质量的音乐。然而，在古典音乐领域，仅依靠统计规律生成音乐仍然存在一些问题：

- 缺少明确的和声逻辑；
- 难以控制音乐结构；
- 无法解释为什么某个和弦或旋律合理；
- 生成结果容易出现"像音乐但缺少发展"的情况。

因此，本项目尝试探索一种结合音乐理论与神经网络的方法。

---

## 1. 不要一开始就相信端到端生成

最初的想法是：

```
音乐数据 → 神经网络 → MIDI
```

让模型直接学习音乐分布。这种方法确实有效，大规模模型通过大量数据可以学习音色规律、节奏模式、和声习惯、风格特征。

但是在古典音乐生成中，很快发现：

> 模型可以生成符合概率分布的音乐，但不一定理解音乐结构。

例如模型可能知道 `C - G - Am - F` 是常见进行，但它不一定知道为什么这里需要属功能、为什么这里应该形成终止、为什么旋律需要发展和解决。因此，古典音乐生成不能只依赖统计学习。

---

## 2. 音乐数据处理比想象中困难

很多项目默认 `MusicXML/MIDI → 训练模型`。但实际上，符号音乐数据并不是天然适合机器学习。

工具如 music21 可以解析音符、时值、声部、节拍。但是对于音乐语义——和弦功能、调性关系、终止式、乐句结构——自动分析效果有限。

例如一个小节 `E - G - C`，机器可以知道有哪些音，但它不知道这是 C大调主和弦、第一转位、还是经过结构。**音乐理解需要上下文。**

---

## 3. LLM 可以作为音乐数据标注工具

在传统工具无法满足需求后，尝试使用大语言模型辅助音乐分析：

```
MusicXML/Kern → music21解析 → JSON结构化 → LLM分析 → 和弦/旋律/结构标签
```

LLM 的优势：能处理复杂上下文、可以结合前后小节判断、更接近人类音乐分析过程。

但是成本问题非常明显。一次自动化数据处理过程中，由于高并发、未充分缓存、长上下文输入，短时间消耗约 $120 API 费用。

> LLM 适合作为高质量标注工具，而不是无限量的数据处理器。

更合理的方法：少量 LLM 高质量标注 → 训练轻量模型 → 模型自动处理大部分数据 → 困难样本交给 LLM。即 Active Learning。

---

## 4. 和声表示比单纯 chord label 更重要

简单表示 `C / Am / F / G` 信息有限。对于古典音乐，更重要的是：

- **功能**：Tonic / Dominant / Subdominant
- **和弦性质**：V7 / vii°7 / iv
- **位置**：乐句结束
- **张力**：高
- **解决**：回到主和弦

音乐意义并不只存在于音符本身，而存在于音符之间的关系、当前音乐上下文、和声功能。因此，**结构化音乐表示是生成系统的重要基础。**

---

## 5. 和声生成比旋律生成容易

和弦生成（ChordGPTv4, 152M）相对容易获得合理结果（93.5% 准确率）。原因：和声空间较低维，并且存在大量理论约束。

而旋律生成（MelodyGPT, 65M）困难很多。因为旋律不仅需要音准正确、节奏正确、和弦匹配，还需要动机发展、乐句结构、张力变化、情绪表达。

模型可能生成"合法旋律"，但不一定生成"有音乐性的旋律"。

---

## 6. 不要用规则修补神经网络

最初的方法：`神经网络生成 → 规则修正`。例如删除不和弦音、修正大跳、强制终止。

但这种方法存在问题：**规则只能修改结果，不能改变模型的生成逻辑。**

更好的方式：`神经模型生成多个候选 → 音乐理论评分 → 选择最佳结果`。即 **Theory-guided decoding**——让音乐知识在生成过程中引导模型，而非事后修补。

---

## 7. 小项目也应该记录失败

这个项目中很多尝试没有达到预期：

- 直接使用 music21 自动分析和声 → 结果不准（chord_type 全为 'M'）
- 直接生成旋律音符 → 自回归退化（C-G-C-G 振荡）
- 使用大量规则修正旋律 → 治标不治本（12 条规则互相打架）

但是这些失败提供了重要信息：音乐结构难以从符号直接获得、旋律生成比和声生成困难、统计规律与音乐理解之间存在差距。

**研究价值并不只来自最终结果。**

---

## 8. 对未来 AI 音乐系统的思考

未来音乐 AI 可能不会简单走向"更大的模型 + 更多数据 = 更好的音乐"。而可能是：

```
大规模神经模型 + 音乐知识表示 + 可控生成结构 + 人类反馈
```

结合神经网络的创造能力与音乐理论的解释能力，形成更加可控、可理解的音乐生成系统。

---

## 总结

这个项目最终没有成为一个完美的 AI 作曲系统，但它完成了一次完整探索：从"AI 能不能生成古典音乐？"到"AI 是否真正理解音乐结构？"再到"如何让机器获得更加明确的音乐表示？"

过程中得到的最大经验：

> **音乐生成真正困难的地方，不是让机器产生声音，而是让机器理解音乐背后的结构、逻辑和表达。**

很多人看到一个 AI 项目，只看到最终 demo；但这个项目最有意思的部分恰恰是**为什么没有直接用大模型生成，而是一路走到了神经符号方向**。
