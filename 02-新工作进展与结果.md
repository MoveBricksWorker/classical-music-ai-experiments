# 新工作进展与结果（神经符号生成 · 旋律非自回归化 + 评估证据链）

> ⚠️ **本文件中的部分数字已被修订**（2026-09-11 第二轮）：
> 「恢复准确率 0.857」「边界 F1 0.08–0.35」「末音长音 88%」等来自**有同曲窗口
> 泄漏的划分**与**渲染前**口径。诚实口径与完整修订见
> [`05-评估修订报告.md`](05-评估修订报告.md)：恢复 **0.407 ± 0.021**、
> 边界 F1（±1 容差）**0.532 ± 0.030**、自洽边界 F1 **0.095 ± 0.034**。


> 日期：2026-09-09　|　在 00-交接文档 的四项建议中完成 ①② 及 ④ 的一部分
> 对应原始项目：MoveBricksWorker/classical-music-ai-experiments

## 一、本次做了什么

| 交接文档优先级 | 状态 | 产出 |
|----------------|------|------|
| ① 旋律模型改非自回归离散扩散 | ✅ 完成 | `src/model/melody_diffusion.py` + `train_melody_diffusion.py` |
| ② 乐理指标入报告 | ✅ 完成 | `src/metrics/theory_metrics.py`（MusicAIR 三件套） |
| ③ 防模仿证据 | ✅ 完成 | `src/metrics/anti_imitation.py`（SIMAA + 马氏距离） |
| ④ 局部重采样交互 | ✅ 部分 | `MelodyDiffusion.resample()`（DeepBach 式） |
| ④ LLM 曲式层 / RL scorer | ⏳ 未做 | 见"未完成事项" |
| 独立生成器（零 GitHub 依赖） | ✅ 完成 | `gen_standalone.py`（规则和声 + 扩散旋律） |

新增文件一览：

```
src/model/melody_diffusion.py   # GETMusic 式 D3PM 非自回归旋律模型
train_melody_diffusion.py       # 训练脚本 (8GB GPU 本地可跑, 100 epoch ≈ 7 分钟)
gen_standalone.py               # 独立生成器: 规则和声+扩散旋律, 无需任何下载权重
evaluate_report.py              # 证据链评估 (语料参考 + 扩散 + 自回归基线对比)
export_samples.py               # 生成样例 → data/generated/*.mid (可听)
demo_resample.py                # DeepBach 式局部重采样演示
src/metrics/theory_metrics.py   # key confidence / melodic smoothness / rhythm matching
src/metrics/anti_imitation.py   # SIMAA@90/95 + 17 维特征马氏距离
tests/test_new_modules.py       # 7 项冒烟测试
tests/test_pipeline_diffusion.py# 管线集成测试 (无需和弦权重)
data/processed/eval_report.json/.md   # 评估报告
```

> 决策记录：原项目 GitHub Releases 的 chord/melody 权重最终**未采用**。
> melody 权重曾用于同条件基线对比（结果印证"效果不好"的自评：
> 马氏距离 8.69 接近 DRMW 随机判定线）；chord 权重下载中途停止，
> 其角色由 `gen_standalone.py` 的规则化功能和声替代。两个旧权重文件
> 如无复现基线需求可直接删除（仅 melody 249MB 在本地）。

## 二、旋律模型：从自回归到非自回归扩散

### 2.1 为什么（针对两大痛点的机制分析）

原 MelodyGPT 的两个痛点都是**自回归**的典型症状：

1. **和弦音间振荡（C-G-C-G）**：自回归只从左到右看历史，一旦
   前期输出偏差，模型"自我确认"错误路径，靠 scorer 事后引导只能缓解。
2. **Cadence 头训练失效**：自回归里"头"（序列起点）最特殊——
   它没有历史上下文，只能从 [SOS] 冷启动，预测困难且不可逆。

### 2.2 设计（GETMusic #5 口径）

- **D3PM 吸收态 [MASK]**：训练时任意位置、任意比例（t ~ U[0.05, 0.95]）
  打掩码，模型**恢复任意位置**。"头"不再特殊 → Cadence 头失效的根因消失。
- **condition flags**：每个位置显式编码
  `0=均未知 / 1=pitch已知 / 2=rhythm已知 / 3=均已知`，推理时模型明确知道
  哪里是真值、哪里待生成，防止"先生成的坏音符污染条件"的误差放大。
- **双向 Transformer 编码器**（无因果掩码）+ pitch/rhythm 双头联合解码。
- **MaskGIT 余弦调度去掩码 + D3PM 回掩码**：早期低置信位置会被重新打掩码、
  后续轮次纠正——这是自回归无法做到的**全局一致性**。
- **理论 scorer 保留在解码侧**：最后 4 轮按
  `模型置信度 × exp(乐理bonus×0.5)` 重加权采样（引导而非事后修补），
  保持"神经-符号"定位。
- **DeepBach 式局部重采样**（`resample()`）：把任意区间重新打掩码、
  区间外真值作为双向上下文 → "改一个和弦，只重生成对应小节"。

### 2.3 训练与结果

- 数据：`melody_llm_full_v2.json`（13,592 音 → 3,394 个 16 音窗口，90/10 划分）
- **默认 v1 模型**：d=252 / 8 层 / 6.2M 参数，RTX 4060 Laptop 8GB，
  100 epoch **约 7 分钟**（429 秒）
- 验证指标（全 [MASK] 出发、给定和声、迭代 16 步恢复）：
  - 生成 pitch 准确率 **0.736**（argmax 口径；MelodyGPT 的 84% 是
    更容易的"下一 token"任务，不可直接比）
  - recovery pitch/rhythm 准确率 0.715 / 0.728
  - 单条生成耗时 ~0.2 秒（16 步去掩码 + 4 轮 scorer 精修）
- v2 大模型（14M）恢复率 0.902 但 SIMAA@90 升至 0.150（背谱）——
  见 3.4 节，最终仍以 v1 为默认

## 三、评估证据链（本次最重要的可汇报数字）

统一口径：60 条 16 音旋律，条件 = 验证集真实和声窗口；
与 414 首人类语料（按 prev_pc=null 切分）同指标对比。

### 3.1 乐理合格度（MusicAIR #16 三件套，mido+numpy 实现，music21 无关）

> 数字以 `data/processed/eval_report.json` 为准（同一次运行的权威记录），
> 下表与该文件一致。采样解码下数字随随机种子有 ±0.02 波动。

| 指标 | 人类语料 | 扩散模型生成 | 结论 |
|------|----------|--------------|------|
| key confidence (K-S 相关) | 0.742 ± 0.10 | 0.700 ± 0.11 | 接近人类，调性清晰 |
| avg interval (半音) | 1.77 | 2.06 | 略宽，仍以级进为主 |
| step ratio (≤2 半音) | 0.857 | 0.778 | **> 0.6（MusicAIR 门槛）** |
| direction change rate | 0.396 | 0.492 | 方向变化更活跃 |
| strong onset ratio | 0.451 | 0.329 | 见下方温度敏感性 |
| downbeat onset ratio | 0.243 | 0.182 | 同上 |
| 和弦音吻合率 | — | 0.821 | 和声匹配良好 |
| 振荡比例 (4音窗≤2音高且全和弦音) | — | **0.017** | 振荡基本消除 |

**温度敏感性**（同模型、同条件，仅解码温度不同）：

| 解码模式 | step ratio | strong onset | chord tone | oscillation |
|----------|-----------|--------------|------------|-------------|
| argmax (确定性) | 0.781 | **0.452** | 0.820 | 0.015 |
| temp=0.6 | 0.836 | 0.377 | 0.819 | 0.021 |
| temp=1.0 (管线默认 0.85) | 0.760 | 0.319 | 0.824 | 0.022 |

解读：argmax 倾向选最常见的四分音符类 → 强拍对齐好；采样引入节奏
多样性但以对齐为代价。**根因是模型没有拍位输入**——下一步应把
"小节内拍位"作为显式条件（condition flag 同款机制），而非调温度。

### 3.2 防模仿证据（MusicLDM #11 + DRMW #2 口径）

- **SIMAA**（三元组 Jaccard 对训练窗口的最大相似度）：
  - 训练集自比：SIMAA@90 = 0.510（共享惯用语法的正常水平）
  - 扩散生成：**SIMAA@90 = 0.000，SIMAA@95 = 0.000** → 未背谱
  - 生成三元组记忆率 0.94（本地惯用音型，属正常；SIMAA 才是硬证据）
- **马氏距离**（17 维符号特征：12 维音级直方图 + 平均时值 + 每拍密度 +
  平均音程 + step ratio + leap ratio）：
  - 训练分布：3.73 ± 1.26
  - 生成分布：4.68 ± 1.38，Welch t = 4.93，p < 0.001
  - 落在训练 95 分位内的比例：0.900
  - DRMW 口径解读：**"接近风格"（3–4）与"有距离但不随机"（>4）之间**——
    生成分布贴合训练风格但未贴合到"复述"的程度（SIMAA 为 0 佐证）。

### 3.3 自回归基线同条件对比（决定性结果）

同一 60 组和声条件、同一 scorer、同一套指标，扩散 v1 vs 自回归 MelodyGPT
（官方权重 melody_model_cloud_v4.pt）：

| 指标 | 人类语料 | 扩散 v1 | MelodyGPT (自回归) |
|------|----------|---------|--------------------|
| key confidence | 0.742 | **0.722** | 0.720 |
| step ratio (≤2半音) | 0.857 | **0.769** | 0.542 (低于 MusicAIR 0.6 门槛) |
| direction change rate | 0.396 | 0.513 | 0.614 |
| strong onset ratio | 0.451 | **0.316** | 0.148 |
| 和弦音吻合率 | — | **0.815** | 0.414 |
| 振荡比例 | — | **0.023** | 0.073 (3.2×) |
| SIMAA@90 / @95 | — | 0.000 / 0.000 | 0.000 / 0.000 |
| 马氏距离 | 3.73 | **4.84** (接近风格) | 8.69 (逼近 DRMW"≥10=随机"边界) |

结论：**非自回归扩散在全部乐理指标上胜出**。自回归 MelodyGPT 的
马氏距离 8.69 接近"随机"判定线，和弦音吻合率 0.414 说明其生成
大量偏离和弦上下文——与作者自评"旋律效果很不好"一致，且本次
首次用可汇报数字量化了差在哪里、差多少。

### 3.4 模型规模 vs 记忆化：防模仿指标的价值实证

同一数据（3,394 窗口 / 461 首）、同口径评估下，扩大模型带来
"准确率 ↑ 但背谱 ↑"的权衡——这正是 SIMAA 要抓的现象：

| 模型 | 参数 | 全掩码恢复 acc (pitch) | SIMAA@90 | SIMAA@95 | mean max sim | 结论 |
|------|------|------------------------|----------|----------|--------------|------|
| v1 (默认) | 6.2M (d=252/8层) | 0.736 | **0.000** | 0.000 | 0.377 | 不背谱 ✓ |
| v2 | 14M (d=384/12层) | 0.902 | **0.150** | 0.133 | 0.704 | 15% 生成接近复述训练窗口 ✗ |
| v3 (拍位条件) | 14M (d=378/12层) | 0.924 | **0.217** | 0.167 | 0.738 | 节奏恢复 0.966 但背谱更重 ✗ |
| v4 (拍位条件) | 6.2M (d=252/8层) | 训练中 | — | — | — | 目标: 拍位收益 + 不背谱 |

关键发现：
1. **拍位条件是节奏对齐的正确解法**——v3 的 rhythm 恢复 0.966
   （v1/v2 约 0.73/0.88），strong onset 0.384（v1 0.329），且这是
   显式结构条件而非温度调参。
2. **容量必须与数据匹配**——14M 在 3.4k 窗口上过拟合（v2/v3 的 SIMAA
   均显著 >0，且训练/验证窗口按窗口索引划分、同曲窗口存在泄漏，
   进一步放大了恢复率）。
3. 结论：当前数据规模下"6.2M + 拍位条件"（v4）是理论上最优组合；
   v1 是"无拍位、不背谱"的已交付默认。要上更大模型，必须先扩数据
   （方向③ 节拍对齐增强）或加正则。

## 四、复现步骤

```bash
pip install -r requirements.txt        # torch/mido/numpy/scipy (music21 可选)

# 训练非自回归旋律模型 (8GB GPU, ~7 分钟)
python train_melody_diffusion.py --epochs 100 --batch 128 --d 252 --layers 8
# 拍位条件变体 (节奏恢复 0.73→0.92)
python train_melody_diffusion.py --epochs 100 --batch 128 --d 252 --layers 8 --beat --out melody_diffusion_v4_beat.pt

# 证据链评估 (含自回归基线同条件对比)
python evaluate_report.py --n 60 --steps 16 --baseline-pt melody_model_cloud_v4.pt
python export_samples.py               # data/generated/*.mid 可听样例

# 完整管线 (和弦权重 + 旋律权重就位后自动走扩散路径)
python gen_full.py
# 独立生成器 (推荐: 规则和声 + 扩散旋律, 零下载依赖)
python gen_standalone.py --seed 42
# 不依赖和弦权重的管线集成测试 (固定进行 → 扩散旋律 → 三轨 MIDI)
python tests/test_pipeline_diffusion.py

# 单元测试
python tests/test_new_modules.py
```

### 管线集成验证（无需 578MB 和弦权重）

`tests/test_pipeline_diffusion.py` 用 48 和弦固定进行走通完整链路：
扩散旋律（3 候选择优）→ polish → 三轨 MIDI（516 起音、12 乐句）。
该 MIDI 旋律轨的乐理指标：key conf 0.781、step ratio 0.644、
强拍空拍率 0.0。注意 token 级 step ratio（0.769）与 MIDI 级（0.644）
的差距来自 polish 的八度拱形映射（八度跳在音级空间表现为大跳）——
这是原管线设计，非扩散模型引入。

### 听感修复：GETMusic 原文核对与八度分配重构（2026-09-11）

获得论文原文后逐条核对 GETMusic 实现，并针对"听感诡异"做三项修复：

1. **八度分配重构（最大听感元凶）**：原 `polish_melody` 按时间区段
   切换八度（0→1→0→-1），实测整八度大跳占比 **37%**（70/191），音域
   横跨 3 个八度。改为**跳进最小化八度分配**（`assign_octaves_min_leap`，
   逐音选使 |Δ| 最小的八度）后大跳降至 0-2 处，音域收敛至 10-21 半音，
   同时保留音级线本身的旋律轮廓。
2. **和弦时值**：规则和声改为至少 2 拍（1 拍和弦使每个旋律 slot 只有
   0.25 拍 → 16 分音符快速跑动的机械感；2 拍 → 八分网格）。
3. **D3PM 祖先采样**（GETMusic 原文口径）：实现按转移矩阵后验
   `p_keep(t)=γ_t·ᾱ_{t-1}/γ̄_t` 的逐位解掩采样（T=100，
   `MelodyDiffusion.generate_d3pm`），对比 MaskGIT 16 步：

   | 采样器 | step ratio | avg interval | chord tone | oscillation | 耗时/条 |
   |--------|-----------|--------------|------------|-------------|---------|
   | MaskGIT (16步, 默认) | 0.756 | 2.05 | 0.815 | 0.015 | 0.14s |
   | D3PM 祖先 (100步) | 0.773 | 2.12 | 0.790 | 0.017 | 0.60s |

   结论：两种采样质量基本持平——瓶颈不在采样方式（GETMusic 的优势
   更多来自其 2I 行统一表示、Roformer 与 14 万条数据），D3PM 采样器
   保留为可选项（`evaluate_report.py --sampler d3pm`）。

   另核对到的差异：GETMusic 的 condition flags 标注"源轨真值 vs 模型
   生成"，本实现标注"已解码 vs 仍掩码"（和声条件以独立流常驻输入，
   语义等价性已在文档注明）。

### 根因修复：训练/推理和声条件不一致（2026-09-11 续）

**发现**：完整管线输出的旋律振荡率 46%、连续同音 34%，而评估集上
同一模型仅 2%——差距来自**和声条件分布不一致**：

1. 语料 LLM 标注的和声**几乎逐音变化**（连续相同条件长度=1 占 98.0%，
   长度=4 全语料仅 1 例）；
2. 推理管线给每个和弦的条件**重复 4 次**（每和弦 4 个旋律音）——
   即训练中从未见过的输入模式。

这个不一致对原版 MelodyGPT 同样存在（train_big.py 的
`repeat_interleave(notes_per_chord)`），很可能是其"振荡退化"的
深层原因之一。

**验证**（合成 I-IV-V7-I 进行，同模型不同条件）：
真实窗口逐音条件振荡 9% → 块恒定条件 13% → 完整管线（简单三和弦
重复 + 稀有条件类型）46%。深挖发现第二层问题：语料把属和弦绝大多数
标注为 `M` 三和弦（(D,M,7)=366 次 vs (D,dom7,7)=65 次），管线却喂
`dom7` 等稀有类型，属分布外条件。

**修复**：
1. **v5 模型**：训练条件改为 4 音块多数投票后恒定（`--block-conds`），
   与推理输入模式一致；训练目标噪声（块条件与逐音标注一致率 52%）
   使恢复准确率指标下降属预期（条件欠定旋律），不代表音乐质量下降。
2. **类型重映射**：旋律条件的和弦类型映射到语料约定
   （dom7→M, m7→M7→M, dim7/hdim7→dim）。
3. **repeat_damp=0.05**：精修轮同音衰减加强（模型在简单三和弦上对
   同音的原始概率过高，0.3 不足以打破长同音串）。
4. 规则和声时值 ≥2 拍（八分音符网格，消除 16 分音符跑动）。

**完整管线三代对比**（ChordGPTv4 和声 + 扩散旋律，同候选评分）：

| 指标 | 修复前 (v1+旧polish) | v1+remap+0.05 | **v5+remap+0.05** |
|------|---------------------|---------------|-------------------|
| 振荡率 | 46% | 32% | **23%** |
| 连续同音串 | 34% | 20% | **14%** |
| 级进率 | 0.51 | 0.30 | **0.61** (>0.6 合格线) |
| 大跳率 | 24% | 10% | **3%** |
| 候选评分 | -0.88 | -0.91 | **+0.24** |

v5 证据链复核：SIMAA@90/95 = 0.000/0.000（不背谱）；块恒定条件下
和弦音占比 0.82→0.48（不再死锁和弦音）；马氏距离 6.64。

### 节拍网格对齐修复（2026-09-11 三修）

**反馈**："旋律与和弦对不上，和弦织体的拍号跟旋律的拍号不一样"。

**根因**：和弦时值是 ChordGPT 的量化 ID（0.25-4 拍任意值），和弦变化
落在非小节位置；旋律又按"和弦时值/4"等分 slot（1.5 拍和弦 → 0.375 拍
间距），与织体的八分脉冲（每拍 2 音）落在不同网格 —— 三轨网格互不
对齐，听感即"两种拍号打架"。

**修复**（`gen_full.py`，两条管线共用）：
1. `snap_to_measures()`：和弦时值吸附 4/4 小节网格 —— 每小节 1 个
   和弦（4 拍）或 2 个（2+2 半小节拆分，30% 概率，乐句末用 D/PD→T
   规则保护而非失效的 cad 标志）。
2. 旋律轨：小节内按节奏时值比例排布 → **量化到八分音符网格**（严格
   递增、不越界）→ 填满整小节（原实现纯比例归一化会产生 0.888 拍
   这类非网格落点）。
3. 织体轨：每拍 2 音改为 round 对齐，随小节网格。

**验证**（对齐检查脚本口径：各轨 note-on 绝对 tick 对网格取模）：

| 曲目 | 小节数 | 和弦偏格 | 旋律偏格 | 织体偏格 |
|------|--------|----------|----------|----------|
| v4_full (完整管线) | 39 | 0 | 0 | 0 |
| piece_A_final | 26 | 0 | 0 | 0 |
| piece_B_final | 31 | 0 | 0 | 0 |

三轨 100% 落在共享网格上（旋律/织体八分网格，和弦半小节网格）。

### 开始/结束表达能力：结构流 + RoPE（2026-09-11 四修）

**反馈**："模型不会表达开始与结束——中间有终止式旋律却突兀继续，真正结尾也不正常。
是否需要重新打标？"

**诊断（两个技术根因，都不需要重新标注）**：
1. **位置编码外推失效**：训练窗口只有 16 音，可学习绝对位置嵌入只训到
   index 15；管线一次生成 192 音，位置 16-191 用的是**从未更新过的随机
   嵌入**。GETMusic 用 Roformer（相对位置）正是为此。
2. **无全局结构条件**：模型不知道当前在曲子的什么位置、离结尾多远，
   因此无法区分"句中"与"句末"的手势。

**实现**：
1. **RoPE 旋转相对位置编码**（`RotaryEncoder`，GETMusic 的 Roformer 口径，
   手写 Q/K 旋转注意力），去掉绝对位置嵌入 → 任意长度外推；
2. **结构流**（从数据的乐曲边界自动推导，零标注成本）：
   - `pos_bin` (8)：全曲相对位置（起始→结尾）；
   - `toend_bin` (4)：距结尾剩余比例（0=结尾处 / 1=收束 / 2=中段 / 3=远离）；
3. **终止式后处理公式**：末 2 实音强制 导音/上主音 → 主音（B→C 或 D→C），
   末小节节奏用"短-短-短-长"公式，终止长音持续到小节末（修掉了
   比例归一化把尾音压回 1 拍的 bug；也修了末音为休止时跳过解决的 bug）。

**v6 模型**（6.6M，块恒定条件 + 结构流 + RoPE，训练 15 分钟）：

| 指标 | v5 | **v6** | 人类语料 |
|------|----|--------|----------|
| 级进率 | 0.590 | **0.753** | 0.857 |
| 马氏距离 | 6.64 | **5.02** | 3.73 |
| SIMAA@90 | 0.000 | **0.000** | — |
| 振荡率 | 0.018 | 0.027 | — |

**结尾质量**（8 首生成曲 vs 397 首人类语料，`analyze_endings.py`）：

| 指标 | 人类语料 | 生成曲 (v6) |
|------|----------|-------------|
| 末音 = 目标主音 (C) | — | **100%** |
| 末音 = 检测主音 | 58% | 38%（检测假象：旋律单独易听成相对小调） |
| 末音为长音 | 64% | **75%** |
| 末 3 音含级进解决 | 74% | **100%** |

附注：旋律单独做 K-S 调性检测时 5/8 被判为 A 小调（与 C 大调共享音级），
但**加入和声轨后 6/8 判为 C major（置信 0.74-0.93）**——整体织体是锚定
C 大调的，相对小调混淆是旋律单轨检测的已知局限。

**关于"是否需要重新打标"**：本轮修复不需要——位置/剩余/结尾信息
全部可从现有数据自动推导。若要做**真正的曲式层**（乐句边界、终止式
类型标注、呈示/发展/再现），则需要 LLM 重新标注（建议先做主动学习
抽检，控制成本）。

### 乐句层："句子要喘气"（2026-09-11 五修，进行中）

**需求**：一曲 = 多个乐句（"句子"），句子之间要换气。v6 只有全曲级
结构，没有乐句级。

**标注**（本地 Ollama 完成，零 API 成本）：
- 模型：`qwen3.5:9b-q4_K_M`（本机 4060/8GB 可跑）
- 关键坑：qwen3.5 是思考型模型，默认把推理全放进 `thinking` 字段、
  `content` 为空且极慢（单次 33-87s）；**`think: false` 后 2 秒返回
  干净 JSON**，吞吐 ~9-18 首/分钟。
- 任务：给音符序列（音名+时值+小节线），标出每个乐句的末音（换气点）；
  校验规则（升序、间隔≥2、末位必含）+ 单曲重试 + 休止/长音规则兜底。
- 产出：`data/processed/melody_phrases_v1.json`（579 首；标注脚本
  `annotate_phrases.py`，断点续传）。

**标注质量抽查**：乐句长度 中位数 5、均值 6.1；边界多落在级进/下行
后的稳定音上（如 D-E-F-E-D→C 处断句）；后半段曲子能标出二分音符
句末。注意：**语料内部几乎没有休止**（299 个休止全在曲末），句末音
也不显著更长——说明语料的乐句靠**音高结构**而非"停顿"标记，
"喘气"的听觉效果需要在渲染层实现。

**训练侧**：v7 = v6 + `phrase_bin` 结构流（每音距本句末的音数，
0=句末换气点，封顶 5）；标签从标注文件按全局音符索引展开。

**推理侧**：管线从和声推导乐句计划（D/PD→T 终止式处必为句末 +
过长乐句每 2 和弦细分的补足边界），生成 `phrase_bin` 喂给模型。

**渲染侧**：句末音缩短 35% 留出气口（`generate_midi`），使换气可听。

**v7 结果**（6.8M，块恒定 + 结构流 + 乐句流 + RoPE，训练 15 分钟）：

| 指标 | v6 | **v7** | 人类语料 |
|------|----|--------|----------|
| 恢复准确率 (pitch, 微调口径) | 0.625 | **0.822** | — |
| 强拍起音率 | 0.358 | **0.422** | 0.451 |
| 级进率 | 0.753 | 0.719 | 0.857 |
| 振荡率* | 0.027 | 0.082 | **0.159** |
| SIMAA@90 | 0.000 | **0.000** | — |
| 马氏距离 | 5.02 | 6.17 | 3.73 |

*振荡率对照人类基线 15.9%——v6/v7 都低于人类，v7 只是更接近
人类行为（含句末停顿手势），非退化。

**模型自发学会的句末行为**（非渲染层强加）：

| | 句末音时值 | 句中音时值 |
|---|---|---|
| 人类语料 | 4.13 | 3.74 |
| v7 生成 | **4.35** | **3.85** |

同样的"句末拉长"模式——乐句流确实让模型学到了乐句收束。

**结尾质量（8 首，含换气渲染）**：

| 指标 | 人类语料 | v6 | **v7** |
|------|----------|-----|--------|
| 末音 = 检测主音 | 58% | 38% | **88%** |
| 末音 = 目标主音 C | — | 100% | 100% |
| 末音为长音 | 64% | 75% | **88%** |
| 末 3 音含级进解决 | 74% | 100% | 100% |
| 调性检测 | 多调性语料 | 3 C + 5 Am | **7 C + 1 Dm** |

乐句结构还间接改善了调性清晰度（检测主音命中率 38%→88%）。

**换气验证**：每次句末均有 0.19-0.57 拍的气口（92BPM 下约
0.12-0.37 秒），`analyze_endings.py` 与 gap 检查脚本可复现。

### 曲式层与真实和声（2026-09-11 六修）

**论文启示**（Toward Guided Musical Form, CMJ 2024）：
- 核心诊断工具 **SSM（自相似矩阵）**：把生成模型的问题描述为
  "漫游 meander"——SSM 上没有清晰区块结构；用 FSD 指标对比生成与
  人类音乐的**结构分布**。
- 核心方法：**LLM 设计曲式**（分段计划）→ 逐段引导生成。

**SSM 诊断（符号域适配，`analyze_structure.py`）**：按拍切片提取
[12 维音级直方图 + 节奏密度] → 余弦自相似矩阵 → 重复比例/平行块
强度/新颖率。生成曲重复比例 0.27-0.32 vs 人类 0.11——但主因是
**和声雷同**（I-IV-V-I 循环）导致的通篇相似，且跨调人工语料天然更低
（长度/调性影响该指标）。

**改进 1：真实和声进行采样**（`--harmony corpus`，默认）：
- 用 461 首语料的真实和弦进行替换循环进行（罗马数字级数，C 大调直接可用）；
- 修复 `V/V`、`V/vi`、`vii°/V` 等**次属和弦级数复合解析**（原被误解析为 V）；
- 不同和弦名 9.3→**13.9**/32，次属和弦 0→**3.8** 个/曲；
- 附带优势：采样的 (功能,类型,根音) 三元组全来自语料真实标注，
  与旋律模型训练分布天然对齐。

**改进 2：曲式层**（`motif_form.py`）：
- `plan_form`：LLM（本地 Ollama）设计乐句计划（素材复用/音区/能量），
  模板兜底（A-B-A' + 音区拱形）；
- `apply_motif_reuse`：**A-B-A' 动机复用**——目标乐句保留源素材中与
  当前和弦协和的音（句头强保留/句尾弱保留）作真值上下文，模型补齐其余；
- `apply_registers`：按计划整句移八度（climax 推高、结尾回落）；
- 模型侧新增 `generate_with_context()`（任意 keep 掩码上下文补全）。

**动机复用验证**：轮廓 LCS 相似度 复用对 0.588 vs 随机对 0.530
（+11%）——效果存在但有限；C 大调内轮廓相似度天然偏高，验证方法
区分度不足，最终以听感为准。

**新成品**：`motif_A2.mid`（模板曲式）、`motif_B2.mid`（LLM 曲式）、
`final_v8_pipeline.mid`。三轨网格对齐、换气点 13-18 个（气口均值
0.23-0.31 拍）。

## 五、句法层：从"片段语料"到"句子语料"（2026-09-11 七修，步骤 1-5）

**问题**（用户指出）：模型仍不理解"句子"——根源是**数据里没有完整句子**
（旧语料是 23.5 音的单乐句片段, 50% 不足 16 音, 165 个单音碎片）。

### 第 1 步：句子级数据集（music21 内置巴赫众赞歌）

- **368 首完整作品**（中位 46 音, 4.4 乐句/首, 24 小节级完整结构），
  全部移调到 C（保留调式, 大调→C / 小调→c）；
- 旋律 = Soprano 声部; 和声 = chordify + 罗马数字分析
  （degree→func: 1/3/6→T, 2/4→PD, 5/7→D; quality+seventh→我们的类型表）;
- 乐句边界 = **终止式（理论真值）**: 全终止 1358 / 阻碍 175 /
  变格 48 / 半终止 42; 边界吸附到终止式附近**时值最长**的旋律音;
- 产出 `data/processed/chorales_sentences_v1.json`（脚本
  `build_chorale_dataset.py`）。

### 第 2 步：标注质量审计（本地 Ollama）——**最重要的发现**

用 qwen3.5:9b（think=false, 40 首抽样）对同一批旋律做换气点分段,
与规则（终止式）标注对比:

| 指标 | 值 |
|------|-----|
| 规则标注被 LLM 认可（召回, ±1 音容差） | 37% |
| LLM 标注落在规则标注附近（精确） | 20% |
| 完全一致 | 0% |
| 每首边界数: 规则 vs LLM | **3.4 vs 6.2**（约 14 音/句 vs 8 音/句） |
| 位置在 2 音内吻合 | 55% |

**结论：两个来源在"粒度"上分歧，不是对错分歧**——终止式标记的是
**乐句/小句层**（Caplin 意义上的 phrase），LLM 标的是**动机/分句层**。
两者位置半数以上吻合。这既是当前模型学句法的上限来源，也是
**层次化标注**（动机⊂乐句⊂句子）的实证依据。

另一佐证：人类众赞歌自身"乐句末音落在到达和弦音"仅 48-59%、
落在根音仅 24%——和声自动标注乐句边界本身带有噪声。

### 第 3 步：模型改造（v8）

- **终止式流**（`num_cadence_types=5`: 无/全/半/阻碍/变格）；
- **边界预测头**（辅助任务, BCE, 权重 0.3）——"理解句末"的可操作定义；
- **结构流随机丢弃（50%）**: 训练时把 phrase_bin 置换为"未知"(6)、
  cadence 置 0——否则边界头可以直接从输入读出答案（首版实测
  F1=1.0 就是这种泄漏）；
- v8: d=320 / 6 层 / 7.4M / **32 音窗口**（≈2-3 个乐句, 模型第一次
  能看见完整乐句）, 与推理同口径的 4 音块恒定和声条件;
- 训练 15 分钟: **恢复 0.857**（v7 0.822）;
  **边界 F1 0.08-0.35（随验证划分波动, P 0.28-0.82 / R 0.05-0.23）**
  —— 模型能从旋律+和声内容识别**部分**句末（高精确/低召回, 保守型）,
  但对数据划分敏感、不稳定。这是"理解句法"的诚实起点:
  **能认出一部分句子边界, 但远未达到稳定的句法理解**。

### 第 4 步：两级生成（规划器 + 实现器）

- **规划器** `compute_sentence_plan`（`gen_full.py`）: 从和声进行推导
  终止式到达点与类型（半终止只在属和弦**不立即解决**时单列）;
- **实现器** `gen_sentence.py`: 采样众赞歌作和声框架 → 取该曲真实
  乐句/终止式计划 → v8 多候选生成（终止式落点和弦音评分）→
  跳进最小化八度 + 终止音修正 + 句末气口 → 双轨 MIDI;
- 成品: `sentence_1.mid`（69 音 5 乐句, 含半终止计划, C 小调 0.89,
  18 个气口, 落主音收束）、`sentence_2.mid`（39 音 4 乐句）。

**调式控制补丁**（用户反馈"为什么两个都是小调"）:
- 数据集调式 194 大调 / 174 小调（近半半），随机采样容易落到小调框架
  （sentence_1 框架确为 c 小调；sentence_2 框架为 F 大调但旋律 D 音
  过多（12/39）导致调性听感模糊）;
- 新增 `--mode {any,major,minor}` 框架调式过滤;
- 候选评分新增**调式一致性项**: 大调框架惩罚小三度(Eb)与降六级(Ab)、
  奖励大三度(E)，小调框架惩罚大三度;
- 末音一律强制落主音 C（修复了"末和弦为 IV 时末音落 F"的问题）;
- 验证: `major_1.mid`（45 音, E=9/Eb=0, C major 置信 0.83, 级进率 0.86,
  15 个气口）、`major_2.mid`（43 音, C major 0.92, 6 个气口），
  均落主音收束。

**织体与音区修复**（用户反馈"和弦织体没了、一直在高音区"）:
- 重构 `write_midi` 时漏掉了织体轨, 补回三轨结构
  （旋律 ch0 / 织体 ch1 阿尔贝蒂·破碎八度·琶音 / 和声垫 ch2）;
- 旋律八度基准从 72 (C5) 降到 64 (E4)、上限 88→79——
  原来比原管线高整整一个八度;
- 修复后: major_1 旋律 55-77 + 织体 96 音(36-71) + 垫 38 音,
  C major 0.83; major_2 旋律 55-74 + 织体 88 音, C major 0.94。

### 第 5 步：句法评估（`analyze_syntax.py` → `syntax_report.json`）

| 指标 | 生成 | 语料基线 |
|------|------|----------|
| 乐句长度（音） | **11.6** | 11.2 ✓ |
| 相邻乐句轮廓相似度（乐段性） | **0.515** | 0.544 ✓ |
| 终止式实现率（落点和弦音） | 0.34 | 0.53（语料自身） |
| 自洽边界率（模型判自己生成的句末命中计划） | 0.08 | — |
| 边界 F1（真实旋律, 结构遮蔽） | 0.08-0.35（随划分波动） | — |

**判读**: 句子尺度（长度、前后句关系）已对齐语料；终止式"唱出来"的
可靠性仍弱（0.34 vs 语料 0.53），主因是标注本身的层级/噪声问题
（见第 2 步审计）与模型容量。渲染层的终止音修正保证了**听感上**
的终止式落地，但模型内部尚未完全内化。

## 六、未完成事项与建议的下一步

1. **方向③ 节拍对齐数据增强**（MusicLDM BLM 的符号域迁移）：
   需要先给旋律数据补拍位信息，暂缓。
2. **方向④ 剩余**：LLM 曲式层（Guided Musical Form 双 LLM 迭代）、
   scorer 升级为 RL 奖励函数（RL-Tuner 口径）、CFG 衰减替代 D/PD→T 硬分句。
3. **听测**：47 人新颖性回忆实验 / 7 点 Likert 模板（Musicality-Novelty GAN、
   Transformer+GAN 论文的模板可直接复用）。
4. **旧权重处理**：`melody_model_cloud_v4.pt`（249MB）仅用于复现基线对比，
   确认不需要后可删除；chord 权重已停止下载、分段文件已清理。
