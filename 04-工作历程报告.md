# 工作历程报告 —— 从交接项目到句子级神经符号生成

> 记录人：AI 协作开发　|　时间：2026-09-11（一天内完成）
> 起点：`MoveBricksWorker/classical-music-ai-experiments` 交接版
> 终点：句子级神经符号生成系统（音乐理论标注 + 非自回归扩散 + 三轨输出）

本报告按**实际工作顺序**记录每一步：当时面对什么问题、怎么想、做了什么、
结果如何。它是一份"过程档案"，技术细节见《03-全项目技术梳理》与
《02-新工作进展与结果》。

---

## 第 0 步：接手与摸底

**起点状态**：一个能跑通的神经符号音乐生成项目——ChordGPTv4（152M 自回归，
6 维功能和声）生成和弦 → MelodyGPT（65M 自回归）生成旋律 → 规则织体 →
三轨 MIDI。交接文档自评"旋律效果依旧很不好"，并给出四项改进建议
（非自回归扩散 / 乐理指标 / 防模仿证据 / 曲式与交互）。

**第一件事不是改模型，而是读代码 + 摸环境**：
- 通读 `gen_full.py`（605 行）、`train_big.py`、`src/constants.py`、
  `src/model/architectures.py`（1126 行，V1-V5 全部模型）；
- 环境：Python 3.10 + torch 2.6 + RTX 4060 Laptop（8GB）+ music21 9.9.2；
- 数据：461 首/31651 和弦（V4 六维）、13,592 个旋律音（LLM 标注）；
- 模型权重不在本地（GitHub Releases，828MB）。

**结论**：先建可复现的评估基线，否则一切改动无法判断好坏——这决定了
后续所有工作的顺序。

---

## 第 1 步：评估体系（一切判断的地基）

**做了什么**：
1. `src/metrics/theory_metrics.py`（MusicAIR 口径）：
   - key confidence：时值加权音级直方图 × 24 个 Krumhansl-Kessler 调性剖面
     的 Pearson 相关；
   - melodic smoothness：平均音程 / step ratio / 方向变化率 / 大跳率；
   - rhythm matching：强拍起音率 / 长音强拍率 / 强拍空拍率。
2. `src/metrics/anti_imitation.py`（MusicLDM + DRMW 口径）：
   SIMAA@90/95（三元组 Jaccard 最大相似度超阈值比例）、17 维特征马氏距离
   + Welch t 检验。
3. `evaluate_report.py`：三列对比报告（人类语料 / 新模型 / 旧模型）。

**踩到的坑**：
- 语料是**平铺拼接**的（跨曲累计拍位会污染节奏指标）→ 改为按
  `prev_pc=null` 边界逐曲评估再聚合；
- 音级空间最大距离是 6（三全音），MusicAIR 的"大跳 ≥7"永远为 0 →
  阈值改为 ≥5 并在文档注明；
- K-S 检测在短片段上会把 C 大调听成 A 小调（共享音级）→ 需要
  旋律+和声一起检测才稳。

**第一个重要产出**：旧 MelodyGPT 的马氏距离 **8.69**（逼近 DRMW 的
"≥10=随机"判定线）、和弦音吻合率仅 0.414——"效果很不好"从此有了
可汇报的数字。

---

## 第 2 步：旋律模型换成非自回归离散扩散

**判断**：原模型的两大顽疾（和弦音间振荡、Cadence 头失效）不是调参问题，
是自回归机制的必然症状——只看左历史、起点没有上下文。

**做了什么**（GETMusic 式 D3PM，`src/model/melody_diffusion.py`）：
- [MASK] 吸收态训练（随机掩码 + 恢复），消除"特殊位置"；
- condition flags（pitch/rhythm 各自的已知/待生成状态）；
- 双向 Transformer + 双头；MaskGIT 迭代解码 + 理论 scorer 引导；
- 训练 100 epoch 只需 **7 分钟**（6.2M 参数，4060 8GB）。

**结果**：同条件对比下全面超过旧模型（级进率 0.769 vs 0.542、和弦音吻合
0.815 vs 0.414、振荡 0.023 vs 0.073、马氏距离 4.84 vs 8.69）。

**过程中发现**：14M 大模型恢复准确率更高（0.902）但 **SIMAA@90=0.15
（背谱）**——防模仿指标第一次直接用于模型选型，最终定 6.2M。

---

## 第 3 步：六个根因（本项目最有价值的发现）

在把模型接进完整管线后，发现**管线输出的振荡率 46%，而评估集上同一模型
只有 2%**——同一个模型差 20 倍，问题必然在输入。顺藤摸瓜挖出六个问题：

1. **训练/推理条件分布不一致**：语料和声标注 98% 逐音变化，管线却按
   和弦块重复 4 次喂条件。**原版 MelodyGPT 有一模一样的写法**——这
   很可能就是它振荡顽疾的深层原因。修复后管线振荡 46%→23%。
2. **位置编码外推失效**：可学习绝对位置只训过 0-15 号位置，生成 192 音
   时后面用的是**从未更新过的随机嵌入**（GETMusic 用 Roformer 正是为此）。
3. **稀有条件分布外**：语料把属和弦标成 M 三和弦（366 vs 65 次），
   管线喂 dom7 触发同音嗡鸣。
4. **容量与数据必须匹配**：14M 在小数据上背谱（SIMAA 抓出），6.2M 才是
   正确规模。
5. **数据里没有完整句子**：旧语料全是 23.5 音的单乐句片段。
6. **"句子"标注本身有层级歧义**（后文第 7 步审计发现）。

---

## 第 4 步：听感工程（用户反馈驱动的七轮修复）

每一条用户反馈都指向了具体的代码根因：

| 反馈 | 根因 | 修复 | 验证 |
|------|------|------|------|
| "诡异" | `polish_melody` 按时间区段切八度，37% 是整八度大跳 | 跳进最小化八度分配 | 大跳 37%→0-2 处 |
| "拍号对不上" | 和弦时值非节拍化（0.25-4 拍任意值）+ 旋律按时值÷4 等分 | 和弦吸附 4/4 小节 + 旋律量化八分网格 | 三轨 0 偏格 |
| "不会表达开始结束" | 位置外推 + 无全局结构条件 | RoPE + 结构流 + 终止式公式 | 末音落主音 12%→100% |
| "句子要喘气" | 无乐句级结构 | 乐句流 + 句末 35% 气口 | 模型自发学会句末拉长 |
| "和声雷同/无曲式" | I-IV-V-I 循环、无动机发展 | 真实语料进行采样 + 曲式层（LLM 计划 + A-B-A' 动机复用） | 和弦多样性 9.3→13.9/32 |
| "两个都是小调" | 数据大小调近半半 + 旋律调式倾向弱 | `--mode` 过滤 + 调式一致性评分 | C major 置信 0.83/0.94 |
| "织体没了、高音区" | 重构时漏织体轨；八度基准高一个八度 | 补回三轨 + 基准 72→64 | 三轨齐全、声部平衡 |

**方法上的体会**：用户听感反馈 → 定位到代码根因 → 用脚本量化验证
（网格偏格数、大跳数、气口长度）——"感觉不对"要能翻译成"指标不对"。

---

## 第 5 步：本地 LLM 标注（Ollama）

**需求**：乐句结构需要标注，但 API 成本高。用本机 Ollama。

**踩到的关键坑**：qwen3.5 是**思考型模型**——默认把推理全写进 `thinking`
字段、`content` 为空，单次 33-87 秒。加 **`think: false`** 后 2 秒返回
干净 JSON，吞吐 9-18 首/分钟。全程零 API 成本。

**产出**：579 首旧语料的乐句边界标注（380 首 LLM + 199 首规则兜底），
用于 v7 的乐句流——模型由此**自发学会句末拉长**（4.35 vs 句中 3.85，
人类语料同模式 4.13/3.74），结尾命中主音率从 38% 升到 88%。

---

## 第 6 步：承认"数据里没有句子"，换数据

**问题**（用户提出）：模型还是不理解"句子"。

**诊断**：查数据发现——旧语料的"曲子"中位数只有 16 音、50% 不足 16 音、
**165 个单音碎片**，且结尾多是"休止+长音"（终止式切分痕迹）——这些
是**单乐句片段**，不是完整句子。我们此前标注的"乐句"（平均 5.5 音）
其实是**动机层**。模型从未见过一个完整句子，不可能学会句子。

**解决**：发现 music21 **离线自带 373 首巴赫众赞歌**——完整作品
（12-24 小节、4-5 个乐句）、乐句由终止式明确界定。构建了
**368 首句子级数据集**（`build_chorale_dataset.py`）：
- 旋律 = Soprano；和声 = chordify + 罗马数字分析；
- 乐句边界 = **终止式理论真值**（全 1358 / 阻碍 175 / 变格 48 / 半 42），
  边界吸附到附近时值最长的旋律音；
- 全部移调到 C（保留调式）。

---

## 第 7 步：标注质量审计与层级发现

**做了什么**：用本地 Ollama 对 40 首独立做换气点分段，与规则（终止式）
标注对比审计。

**发现（重要）**：规则标 3.4 句/首、LLM 标 6.2 句/首，位置在 2 音内吻合
55%，完全一致 0%。**这不是对错分歧，是层级分歧**——终止式标的是
**乐句层**（约 14 音），LLM 标的是**动机层**（约 8 音）。佐证：人类语料
自身"乐句末音落在期望和弦音"也只有 48-59%。

**意义**：这是"模型学不会句法"的**上限来源**，也是层次化标注
（动机 ⊂ 乐句 ⊂ 句子）的实证依据。

---

## 第 8 步：句子级模型 v8（终止式流 + 边界预测头）

**做了什么**：
- 新增**终止式条件流**（无/全/半/阻碍/变格）；
- 新增**边界预测头**（辅助任务）——"模型是否理解句子"的可操作定义；
- **踩坑与修复**：首版边界 F1=1.0 是**评估泄漏**（边界可从 phrase_bin
  输入直接读出）→ 加"结构流随机丢弃 50%"，让边界判断必须来自旋律+
  和声内容；
- 32 音窗口（≈2-3 个乐句，模型第一次能看见完整句子）；
- 训练 15 分钟：**恢复 0.857**（超过 v7 的 0.822），
  边界 F1 0.08-0.35（高精确/低召回，随划分波动）。

**诚实结论**：模型能认出一部分句末，但远未达到稳定的句法理解。

---

## 第 9 步：两级生成与句法评估

**规划器 + 实现器**（`compute_sentence_plan` + `gen_sentence.py`）：
- 规划器从和声进行推导终止式到达点与类型（半终止仅在属和弦不立即解决
  时单列）；
- 实现器：众赞歌和声框架 → 真实乐句/终止式计划 → v8 多候选生成
  （终止式落点评分 + 调式一致性）→ 跳进最小化八度 + 终止音修正 +
  句末气口 → **三轨 MIDI**。

**句法指标**（`analyze_syntax.py`）：

| 指标 | 生成 | 语料基线 |
|------|------|----------|
| 乐句长度 | 11.5 音 | 11.4 ✓ |
| 相邻乐句轮廓相似度（乐段性） | 0.513 | 0.565 ✓ |
| 终止式实现率 | 0.35 | 0.53（语料自身） |
| 边界 F1 | 0.08-0.35 | — |

**判读**：句子尺度已对齐语料；终止式"唱出来"的可靠性仍弱。

---

## 第 10 步：最终交付与验证

**成品**（三轨：旋律/织体/和声垫）：
- `major_1.mid`：45 音 / 5 乐句，终止式计划"变格→全→阻碍→全→全"，
  C major 0.83，级进率 0.86，旋律音域 55-77；
- `major_2.mid`：43 音 / 4 乐句，C major **0.94**，级进率 0.79；
- 另有 `sentence_1/2.mid`（含小调框架）、`final_*` 系列（旧管线）。

**交付文件**：
- 代码：`melody_diffusion.py`、`gen_sentence.py`、`train_v8_chorales.py`、
  `build_chorale_dataset.py`、`annotate_phrases.py`、`audit_phrases.py`、
  `analyze_*.py`、`evaluate_report.py`、`motif_form.py` 等；
- 数据：`chorales_sentences_v1.json`（368 首）、`melody_phrases_v1.json`、
  各评估报告 JSON；
- 模型：v1-v8 八个检查点（35-82MB/个，共约 315MB）；
- 报告：本报告 + 02（技术日志八修）+ 03（全项目梳理）。

---

## 复盘：十条经验

1. **先建评估，再改模型**——否则无法判断改动是改进还是退步；
2. **机制层面的诊断优先于调参**：振荡是自回归的症状，换机制而不是调温度；
3. **训练/推理一致性是隐形的深渊**——六次问题里三次与它有关
   （条件块、位置编码、结构流泄漏）；
4. **数据质量决定上限**：没有完整句子的数据训不出懂句子的模型；
5. **防模仿指标有两用**：既是论文证据，也是选型工具（抓背谱）；
6. **听感反馈要翻译成指标**："诡异"= 八度大跳率，"拍号对不上"= 网格偏格数；
7. **本地小模型足够做批量标注**（think=false 是关键坑），零成本；
8. **评估本身会泄漏**：边界 F1=1.0 时必须先怀疑泄漏，而不是庆祝；
9. **诚实记录失败**：14M 背谱、审计发现的层级歧义、终止式实现率不达标
   ——这些是论文里最有价值的部分；
10. **重构会丢东西**：补织体轨、降八度——每轮重构后要用清单核对
    "旧版的哪些能力必须保留"。

---

*下一步（见 03 文档第九节）：层次化标注（动机层 + 乐句层双条件流）、
边界 F1 稳定化、终止式实现率提升、扩展语料、人类听测。*
