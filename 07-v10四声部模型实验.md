# v10 实验报告 —— 四声部神经符号生成

> 时间：2026-09-12　|　产物：`src/model/satb_diffusion.py`、`train_v10_satb.py`、
> `v10_aug_vl20.pt`、`data/processed/v10_multisplit.json`
> 前置：迭代 8（四声部数据集与织体标注，见 `06-数据集与织体标注.md`）
>
> **一句话**：把建模单位从"一条旋律的音符"换成"一个纵向切片"，模型第一次能写出
> **四个声部同时进行**的音乐；配和声音高准确率 **0.877±0.056**（给定和声与
> Soprano，补 Alto/Tenor/Bass），声部进行违规从人类的 50 倍降到约 7 倍。

---

## 一、为什么要有 v10（v9 的结构性上限）

| | v9 | v10 |
|---|---|---|
| 建模单位 | 一个旋律音（单声部序列） | 一个**纵向切片**（含 4 个声部的音高/时值） |
| 能学到的 | 旋律的局部走向、与和声的配合 | **声部进行、对位、织体**（同时进行的几条线如何互相让路） |
| 输出 | 1 条旋律 + 规则伴奏（阿尔贝蒂…） | 4 个声部（合唱织体） |
| 数据 | 18,547 音符（只有 Soprano） | 85,699（众赞歌四声部）+ 583,707（Palestrina） |

v9 的所有听感问题（伴奏风格穿越、句法弱、不像"真音乐"）都指向同一件事：
**它没有声部的概念**。v10 直接从这个缺口切入。

---

## 二、表示与模型

**表示**（`chorales_satb_v2.json` / `palestrina_satb_v1.json.gz` 已是这个形状）

```
每首曲子 = 一串纵向切片, 每个切片存四个声部的 (音高, 时值)
    pitch  [4, T]   绝对 MIDI（36–88）或 REST
    rhythm [4, T]   时值 bin（0–7）或 HOLD（8 = 延音、不重新起音）
```

**三个刻意的设计决定**

1. **绝对音高而非音级**（55 类 = 53 个半音 + 休止 + MASK）：四声部写作里
   "低音在 C3 还是 C5"就是音乐本身；只用音级会丢掉声部间距/交越，相关指标也算不出来。
2. **条件流相加而非拼接**：v9 用拼接（每流 `d/流数` 维），导致 `d` 必须被流数整除
   （实测 384/512/768 全不行，只有 400/480/640/800 可以）；改成各流独立嵌入到 `d` 维
   再**相加**后宽度自由。
3. **每声部独立输出头**：S/A/T/B 四套，比共享头更容易学到"低音要跳、女高音要唱"。

**模型**：`SATBDiffusion`（22.4M，d=480/8 层/8 头/RoPE），复用 v9 已验证的 D3PM
吸收态掩码与 MaskGIT 解码框架。

**三种训练任务，同一个损失**（每窗随机选一种掩码模式）

| 模式 | 占比 | 含义 |
|---|---|---|
| `harmonize` | 40% | 保留 1–2 个声部（通常含 Soprano），补其余 —— 经典"配和声" |
| `infill` | 30% | 挖掉一段连续切片上的所有声部 —— 续写/补全 |
| `random` | 30% | 逐 (声部, 切片) 随机挖 —— 通用掩码恢复 |

---

## 三、评估协议（以及它的边界）

- **按曲分组划分**（22 首留出），**3 个划分取均值±标准差**（迭代 7 的教训：
  单划分的数字不可信 —— 基线模型在不同划分上是 0.56 / 0.80 / 0.74）；
- 任务：只给 Soprano（+ 和声条件），生成 Alto/Tenor/Bass，与真值逐音比对；
- 声部进行指标：平行五/八度、交越、间距、覆盖、音域，**全部配真巴赫基线**；
- ⚠️ **边界**：和声条件是 `chordify` 从真实四声部反推的，所以这是
  "**给定和声的实现（realization）**"而不是"从零配和声（harmonization）"；
  任务比论文里的配和声容易，数字不能直接与文献对比。

---

## 四、结果（3 个划分 × 64 窗口）

| 模型 | 音高准确率 | 节奏准确率 | 平行五/100 | 平行八度/100 |
|------|-----------|-----------|-----------|-------------|
| 基线（无增广） | 0.699 ± 0.104 | 0.788 | 0.422 | 1.447 |
| **+ 移调增广 ±2** | **0.877 ± 0.056** | 0.943 | 0.185 | 0.551 |
| **+ 移调增广 + 声部进行惩罚** | 0.876 ± 0.056 | 0.944 | **0.174** | **0.466** |
| + 移调增广 + 弱惩罚（0.5） | 0.825 ± 0.051 | 0.885 | 0.342 | 0.791 |
| **真巴赫（同口径）** | — | — | **0.022** | — |

逐声部（最优模型）：A 0.83 / T 0.82 / B 0.85。

**解码侧再叠加多候选择优**（`analyze_v10_decode.py`，不看真值，只用
"和弦音吻合 + 声部进行违规"给 8 个候选打分）：

| 策略 | 音高 | 平行五 | 平行八度 |
|------|------|--------|----------|
| 单步 argmax | 0.866 | 0.243 | 0.628 |
| **8 候选 + 乐理择优** | 0.861 | **0.154** | **0.441** |

→ 两步可以**叠加**：准确率几乎不掉（-0.005），平行五再降 37%。
与人类的差距由此从 50 倍缩到约 **7 倍**。

---

## 五、四个发现（每个都值得单独记）

### 1. 解码器的"训练/推理不一致"（与 v5 条件块 bug 同源）

配和声准确率随解码**步数单调下降**：

| 解码 | 单步 argmax | 贪心 2 步 | 4 步 | 8 步 | 采样 16 步 |
|---|---|---|---|---|---|
| 准确率 | **0.622** | 0.484 | 0.348 | 0.230 | 0.139 |

根因：训练时 `known=True` 的位置**永远是真值**，而迭代解码把自己的预测标成
`known` 喂回去 —— 模型把"自己的猜测"当成可信条件，步数越多污染越重。
**修复**：本模型用单步 argmax（与训练用法一致，且便宜 16 倍）。
若要真正做迭代精修，需要给"预测值"单独的 flag 并在训练时做自条件化。

### 2. 移调增广是决定性改进（+0.18）

语料本就全部移调到 C 调，而"给定和声 + Soprano 配内声部"这个任务对**移调等变**
—— 所以增广给的**不是新信息，而是不变性压力**：逼模型学"相对结构"而不是
记住绝对音高。5 倍数据把准确率从 0.699 拉到 0.877，方差减半。

### 3. 语料混合会稀释"有解"的任务

众赞歌 + Palestrina 一起训（88% 窗口来自 Palestrina）时，配和声反而从 0.14 掉到
0.09：Palestrina 没有功能和声标注、内声部本质不可预测，模型被迫去学"通用先验"。
→ 必须先众赞歌（本阶段），再**平衡混合**（已加 `--palestrina-max` 支持）。

### 4. 声部进行可测量化，但软惩罚只能小步改善

- 可微近似惩罚（用音级边缘分布估 `P(前后都是纯五/八度) × P(同向)`）：
  平行五 0.185→0.174、平行八度 0.551→0.466，**准确率不变**；
- 但软惩罚优化的是"**期望**违规数"，与"argmax 序列的**实际**违规数"不等价，
  权重过小（0.5）反而更差 → 真要压违规，解码侧多候选择优更直接有效。

---

## 六、局限

1. 和声条件是反推的，"从零配和声"未做（需要模型自己推断和声）；
2. 评估集只有 22 首（3 个划分），且**没有人类听测**；
3. 交越率 1.3/100 vs 真值 0.62 —— 声部交越比巴赫多一倍，未专门处理；
4. 时长：只训练了 64 切片窗口（≈16 小节），更长作品的全局结构未建模；
5. Palestrina 的 58 万音符尚未真正用上（平衡混合实验进行中）。

## 七、复现

```bash
# 训练（本机 4060 约 12 分钟 / 4090 约 3 分钟）
python train_v10_satb.py --corpus chorales --epochs 300 --win 64 --stride 8 \
    --aug-transpose 2 --vl-weight 2.0 --patience 5 --tag v10 --out v10.pt

# 多划分评估（3 个划分均值±标准差）
python train_v10_satb.py --corpus chorales --eval-only --ckpt v10.pt --eval-n 64

# 解码侧多候选择优
python analyze_v10_decode.py --ckpt v10.pt --candidates 8

# 导出四声部 MIDI（生成版 + 真值版并排，便于 A/B 试听）
python train_v10_satb.py --corpus chorales --export-only --ckpt v10.pt --n-export 5
```

## 八、下一步

1. **平衡混合 + Palestrina**：用 `--palestrina-max` 把对位语料按 ~50% 掺进来，
   目标是降低交越率、提高声部独立性（实验进行中）；
2. **从零配和声**：把和声条件也挖掉一部分（模型自己推和声），评估"和声正确率"；
3. **接进生成管线**：把 v10 的四声部输出接到 `gen_sentence.py`，替掉
   "旋律 + 阿尔贝蒂低音"的三轨；
4. **人类听测**：生成版 vs 真巴赫的盲听 A/B（这是唯一能回答"到没达到期望"的方法）。
