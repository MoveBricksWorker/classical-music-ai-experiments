# 评估修订报告 —— 验证集泄漏、渲染层与证据链修复

> 时间：2026-09-11（同一天的第二轮）　|　起点：`02/03/04` 三份报告与 v8 模型
> 目的：修掉外部评审指出的三个问题，并把所有数字换成**可复现的诚实口径**。
>
> **一句话结论**：v8 的"恢复准确率 0.857"来自**同曲窗口泄漏**，同一配方在
> 按曲分组划分下只有 **0.41±0.02**；渲染层此前把模型学到的节奏全部压掉；
> 边界 F1 从"0.08–0.35 波动"变为 **0.53±0.03（±1 音容差）**的稳定值。
> 修复后防模仿证据反而更强：生成旋律比**人类留出曲**更不像训练语料。

---

## 一、问题 1：验证集泄漏（最严重）

### 1.1 诊断

滑窗 `win=32 / stride=6` ⇒ 同曲相邻窗口重叠 **26/32 = 81%**；
而旧划分是"全局 shuffle 后按样本取 90/10"（`train_v8_chorales.py`），
**没有按曲分组**。实测（复现同一 shuffle 种子）：

| 统计量 | 值 |
|--------|-----|
| 验证窗口数 | 135 |
| 与某个训练窗口**同曲且时间重叠**的验证窗口 | **129（96%）** |
| 整个数据集里"只在验证集出现"的曲 | 5 / 368 |

即：验证窗口不是"模型没见过的音乐"，而是"模型见过的曲子里没被选中的窗口"。

### 1.2 定量分解（`analyze_leakage.py`）—— 决定性实验

固定训练集为同一批**曲**、同一批窗口（每首 `start%12==0`），只改变验证窗口：

| 验证窗口 | 恢复 t50 | 恢复 t85 | 全掩码生成 | 边界 F1@.5 |
|----------|----------|----------|-----------|-----------|
| 同曲的 `start%12==6` 窗口（**见过曲子、没见过窗口**） | **0.803** | 0.783 | **0.702** | 0.733 |
| 完全没见过的曲的全部窗口（**按曲分组口径**） | **0.412** | 0.378 | **0.326** | 0.487 |
| 膨胀量 | **+0.390** | +0.405 | **+0.377** | +0.246 |

这是**同一个模型、同一份训练数据**，差别只在"验证窗口是否与训练窗口同曲重叠"
（重叠率 81%）。所以虚高**全部来自泄漏本身**，与"训练曲目数量"无关：

- 恢复准确率虚高约 **+0.39**（0.41 → 0.80，约 +95%）；
- 全掩码生成虚高约 **+0.38**（0.33 → 0.70，约 +115%）；
- 边界 F1 虚高约 **+0.25**（0.49 → 0.73）。

（1.3 节的 A/B/C 对照里边界 F1 看起来几乎不变，是因为那里同时改变了
"训练曲目数"（363 vs 331 首）——两个效应部分抵消。本节的分解实验才是干净口径。）

### 1.3 对照实验（同代码、同超参、只改划分）

`train_v9_chorales.py`，120 epoch，单次划分：

| 配置 | 划分 | 恢复 t50 | 边界 F1@.5 | 边界 F1±1 | P | R | 全掩码生成 |
|------|------|----------|-----------|-----------|---|---|-----------|
| A：v8 原超参 | **按曲分组（诚实）** | 0.434 | 0.539 | 0.557 | 0.542 | 0.535 | 0.276 |
| B：A + 边界类加权 | **按曲分组（诚实）** | 0.412 | 0.548 | 0.607 | 0.505 | 0.597 | 0.320 |
| C：v8 原超参 | 样本级随机（**旧口径，泄漏**） | **0.809** | 0.584 | 0.604 | 0.633 | 0.542 | **0.727** |

**泄漏的膨胀量：恢复 +0.38（约 +88%）、生成 +0.45（约 +160%）；边界 F1 基本不变**
（边界任务要求从内容读出答案，泄漏窗口不是被记住的窗口）。

### 1.4 旧权重复核（v8 检查点）

| 评估口径 | 恢复 | 边界 F1 | 全掩码生成 |
|----------|------|---------|-----------|
| v8 原评估口径（泄漏 val，随机掩码比例） | 0.738 | 0.082 | 0.645 |
| 同权重、在"按曲分组 val"上 | 0.897 | 0.088 | — |
| 文档记载 | 0.857 | 0.08–0.35 | — |

两点结论：
1. **0.857 不可复现**（复现值 0.738）；
2. 0.897 看着更高，但 v8 训练覆盖了 363/368 首曲的**全部窗口**，
   那些"干净 val"窗口其实全在它的训练集里 —— 不能当作泛化数字。

### 1.5 修复后的诚实数字（5 折按曲交叉验证，配置 B）

| 指标 | 均值 ± 标准差 |
|------|---------------|
| 恢复 t=0.15 / 0.50 / 0.85 | 0.493 ± 0.017 / **0.407 ± 0.021** / 0.317 ± 0.026 |
| 边界 F1 @0.5（严格） | 0.491 ± 0.032 |
| 边界 F1（±1 音容差） | **0.532 ± 0.030** |
| 边界 F1（±2 音容差） | 0.583 ± 0.026 |
| 边界 精确 / 召回 | 0.458 ± 0.054 / 0.536 ± 0.035 |
| 全掩码生成（条件含真实乐句计划） | 0.264 ± 0.050 |

**边界 F1 的"稳定化"目标达成**：旧报告"0.08–0.35，随划分波动"的来源是
（a）泄漏划分 +（b）选点用严格 F1、噪声大。现在改为"±1 容差 + 5 折
均值±标准差"，标准差 0.03，可汇报。
（容差在乐句边界判定里是合理的：边界本身有 ±1 音的模糊性。严格口径同时报告。）

---

## 二、问题 2：渲染层把模型学到的节奏压掉了

### 2.1 旧口径

`write_midi` 把每个旋律音时值截断到 `min(bin, 1.0)` 拍，句末再 ×0.65。
**实测旧成品 `major_1/2.mid`：全部音符 ≤1 拍，末音长音 0%。**
也就是说 v7/v8 报告里的"模型自发学会句末拉长（4.35 vs 3.85）"、
"末音长音 88%" 只存在于模型内部输出，**从未进入 MIDI 成品**。

### 2.2 新口径（`render_melody_timing`）

小节仍锁 4 拍（4 音，和声/织体不偏格），但：

1. 小节内按模型节奏 bin **比例分配** 4 拍；
2. 起点吸附 1/4 拍（十六分）网格，时值 = 相邻起点之差（**天然不重叠**）；
3. 相邻音被吸到同一格时后者并入前者（避免零时值）；
4. 句末音留 `--breath`（默认 0.25 拍）气口。
5. 旧口径保留为 `--render slot`，可复现历史成品。

### 2.3 前后对照（渲染后 MIDI 实测）

| | 旧渲染（major_1/2） | 新渲染（v9_piece_1–3） | 人类语料 |
|---|---|---|---|
| 旋律时值范围 | 全部 ≤1.00 拍 | 0.50 – 3.75 拍 | — |
| 末音长音（≥1.5 拍） | **0%** | **100%** | 64% |
| 末音=主音 / C 大调置信 | 100% / 100% | 100% / 100% | 58% / — |
| 音符合并 | — | 0 处 | — |
| 网格 | 1 拍槽位 | 0.25 拍网格 | — |

回归测试 `tests/test_rendering.py`（3 项）：比例分配/气口/网格/不重叠/
三轨齐全/句末长音 —— 防止再退化。

---

## 三、问题 3：证据链没有覆盖当前模型

v8 从未做防模仿评估（表里是"—"），自洽边界率（模型给自己生成的旋律划句）
也从未报告。本轮补全（5 折平均）：

| 指标 | 生成 | 人类（留出曲） | 训练语料 |
|------|------|----------------|----------|
| SIMAA@90 / @95 | 0.000 | 0.000 | — |
| 三元组最大相似度（均值） | **0.342** | **0.583** | — |
| 三元组记忆率 | 0.977 | **1.000** | — |
| 马氏距离（17 维符号特征） | 4.02 – 5.13 | 3.98 ± 1.04 | 3.59 |
| **自洽边界 F1** | **0.095 ± 0.034**（P0.13/R0.08） | — | — |
| 终止式实现率 | 0.344 ± 0.079 | 0.53（语料自身） | — |
| 前后句轮廓相似度（**同一批窗口**配对） | 0.503 ± 0.032 | 0.540 ± 0.029（真值旋律） | — |
| 乐句长度（同一批窗口） | 8.969 ± 0.253 | 8.969 ± 0.253 | — |

**关键读法**：
- 生成旋律与训练语料的最大相似度（0.342）**低于**人类留出曲（0.583）——
  模型没有背谱，甚至比真实巴赫更"不像语料"；
- 马氏距离与人类几乎重合（4.0–5.1 vs 3.98），远低于 DRMW 的"随机"线 10；
- **自洽边界率 0.095 是当前最诚实也最刺眼的数字**：模型能认出一部分
  句末（对真值旋律 F1±1 = 0.53），但对自己写出的旋律几乎划不出句
  ——"理解句法"仍未达成，这是下一阶段的主指标。

### 分终止式类型的边界召回（fold0）

| 类型 | 真值数 | 召回 |
|------|--------|------|
| 全终止 authentic | 565 | 0.51 |
| 阻碍 deceptive | 90 | 0.49 |
| 变格 plagal | 30 | 0.47 |
| **半终止 half** | 24 | **0.12** |

半终止（弱收束）几乎认不出——与"半终止本身模糊、且样本少"一致，
是层次化标注（动机层/乐句层）要解决的同一问题。

---

## 四、代码与工程修复

| 项 | 说明 |
|----|------|
| `src/chorale_conditions.py` **新增** | 条件流编码的**唯一实现**（4 音块恒定/乐句/终止式/结构流）+ 按曲分组划分 + k 折。此前训练、生成、评估三处手抄同一套逻辑——项目第 1 条铁律"训练/推理一致"靠复制粘贴维持，是隐患。迁移已验证与原实现**逐窗口完全一致**（1263 个唯一窗口多重集相等） |
| `train_v9_chorales.py` **新增** | 按曲分组/样本级划分可选、边界头正类加权、多种子/k 折、阈值扫描/±容差/分类型报告 |
| `evaluate_chorale_model.py` **新增** | 完整证据链：恢复/边界/生成/自洽/终止式/防模仿（含人类基线），支持多折汇总 均值±标准差 |
| `analyze_leakage.py` **新增** | 泄漏分解实验（同曲未训窗口 vs 未见曲） |
| `tests/test_rendering.py` **新增** | 渲染契约回归测试 |
| `src/model/melody_diffusion.py` | 修：`__main__` 冒烟测试解包错误（此前必崩）；scorer 精修只作用于 batch 0（B>1 时静默失导）；`generate_d3pm` 用 batch 0 的邻居做 prev_pc；训练损失新增 `boundary_pos_weight` |
| `gen_sentence.py` | `--pieces` 从死参数改为真正生成 N 首；新增 `--model/--render/--breath`；默认模型优先 v9；改用统一条件编码 |
| `analyze_endings.py` | 新增 `--midi`：可直接评估**渲染后**成品（此前只能评估模型内部输出） |
| `src/metrics/anti_imitation.py` | 文档串 leap ratio ≥7 → ≥5（与代码一致） |
| `.gitignore` | 新增测试产物规则 |

**主模型**：`melody_diffusion_v9_chorales.pt`（= 按曲分组 90/10 训练，
d=320/6L/7.4M，20 秒/epoch，2.5 分钟/次）；5 折权重
`melody_diffusion_v9_fold{0..4}.pt`；泄漏对照 `melody_diffusion_v9_leaked.pt`。

---

## 五、诚实标注：哪些是生成、哪些是检索、哪些是规则

这是之前报告最容易让读者误判的地方（"神经符号"里的符号层其实是**检索**）：

| 组件 | 来源 | 说明 |
|------|------|------|
| 和声框架（func/type/root） | **语料检索** | 从 368 首众赞歌里采一首的逐音和声（4 音块恒定） |
| 乐句/终止式计划 | **语料检索** | 该曲的真实乐句边界与终止式类型 |
| 旋律音高/节奏 | **模型生成** | v9 扩散模型（非自回归） |
| 终止音修正 | **规则** | 到达点/末音强制落主音或和弦根音 |
| 八度分配 | **规则** | 跳进最小化 |
| 三轨织体 | **规则** | 阿尔贝蒂/破碎八度/琶音轮流 |
| 句末气口、节拍网格 | **规则** | 本轮渲染层 |

推论：**"乐句长度 11.5 vs 语料 11.4 ✓" 不是模型学出来的**，而是计划
本身就来自语料（此前报告把它当作模型能力证据，本轮修正）。
本轮用**同一批窗口的配对基线**重测，直接证实了这一点：

| 指标（同一批 32 音窗口） | 模型生成 | 真值旋律 | 说明 |
|--------------------------|----------|----------|------|
| 乐句长度 | 8.969 ± 0.253 | **8.969 ± 0.253** | **完全相同** —— 长度由检索来的计划决定，与模型无关 |
| 前后句轮廓相似度 | 0.503 ± 0.032 | 0.540 ± 0.029 | 这一项才是模型属性，生成略低于真值 |

模型侧真正的句法指标是自洽边界 F1 = **0.095**。

---

## 六、数字总表（旧口径 → 新口径）

| 指标 | 旧报告（v8） | 本轮（v9，诚实口径） |
|------|--------------|----------------------|
| 恢复准确率 | 0.857（泄漏） | **0.407 ± 0.021**（t=0.5，5 折） |
| 边界 F1 | 0.08–0.35（不稳） | **0.532 ± 0.030**（±1 容差） |
| 全掩码生成 | 未报 | 0.264 ± 0.050 |
| 自洽边界 F1 | 未报（旧代码实测 0.000） | 0.095 ± 0.034 |
| 终止式实现率 | 0.35 | 0.344 ± 0.079（语义未变，仍有噪声） |
| 末音长音（成品 MIDI） | 0%（声称 88%） | **100%**（人类 64%） |
| 防模仿 SIMAA@90 | —（未做） | 0.000（人类 0.000） |
| 马氏距离 | —（未做） | 4.02–5.13（人类 3.98） |

---

## 七、局限与下一步

1. **自洽边界率 0.095 是核心短板**：模型对"自己写的旋律"几乎没有句法感。
   下一步优先级最高的仍是**层次化标注**（动机层 ⊂ 乐句层）与
   解码侧终止式 scorer，并把自洽边界率作为主验收指标。
2. **恢复准确率降到 0.41 不等于模型变差**——是尺子变准了。若要在诚实口径下
   提升，可行方向：随机移调增广（语料已全部移调到 C，可无损增广）、
   更大数据（众赞歌四声部）、dropout/权重衰减调参。
3. 半终止识别（召回 0.12）与终止式实现率（0.34 vs 人类 0.53）仍弱，
   且人类基线本身受标注噪声影响（"乐句末音落在到达和弦音"仅 48–59%）。
4. 仍未做人类听测；本报告所有"听感"结论都是代理指标。
5. 训练集 368 首、单声部化；分句计划仍取自语料，**自由和声生成**未做。

---

*尺子比尺子上的读数重要。0.41 比 0.857 更有用。*
